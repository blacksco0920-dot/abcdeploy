# 桌面命令面逐命令证据矩阵

## 审计基线

- 当前随应用打包的前端是唯一受支持的 IPC 消费者；没有外部 SDK、插件或远程 IPC 兼容承诺。
- Comet 状态 `base_ref`: `089cdb7470a2f208a179336993fab61791b4bf40`；实施计划 front matter `base-ref`: `24e0fc79f9bda0401362b4e93cb570774b4abfa8`。两者之间只有规划/状态资产，生产命令面未变化；本次实际审计起点 HEAD 为 `77ff7066c945bf204bc56c1783e5e4b246a39dbd`。
- 实际命令：`pnpm --filter @abcdeploy/desktop build`（PASS，仅既有 Vite chunk-size warning）；`node scripts/check-desktop-command-surface.mjs --mode all`（按设计退出 1）；`node scripts/check-desktop-command-surface.mjs --mode all --json`（同一结果对象）。
- 数量：`registered=111`、`source=56`、`bundled=46`、`dynamicInvocations=0`；差异为 `missingRegistrations=0`、`registeredOnly=55`、`bundleOnly=0`、`sourceNotBundled=10`。
- 65 个未进入 bundle 的候选 = 55 个 `registeredOnly` + 10 个 `sourceNotBundled`，均在下表各占一行。另将 `check_registry_credentials` 单独裁决：它在 bundle 中，但字符串只由浏览器回退路径保活，不是 Tauri Feature 消费证据。
- 证据方法：`pnpm codegraph:status` 确认索引最新；对每个 `internalize` 候选运行 `codegraph callers`、`codegraph impact`，并用 `rg -n <symbol> apps/desktop/src-tauri/src` 复核 Tauri 动态注册、同名 Workspace 方法和嵌套 caller。CodeGraph 报告 handler “无 caller”时，以表中明确列出的 `*_inner`、Workspace caller 或保护测试补证。

## 矩阵

| command                                  | feature consumer                                                                                                                                                                                                                                                                  | TypeScript wrapper                                                          | production bundle | Rust internal callers                                                                                                                                                              | data/migration duty                                    | tests                                                                                                                                             | decision      | rationale                                                                                                                       |
| ---------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------- | ----------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- | ------------------------------------------------------------------------------------------------------------------------------- |
| `apply_manifest`                         | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | `src-tauri/src/tests.rs` 的 open/manifest 行为保护，不要求旧 endpoint 存在                                                                        | `delete`      | 无生产消费者，`rg` 仅命令定义/handler；共享 manifest/SSH/Workspace 内核由当前入口另行保留。                                     |
| `begin_deployment_attempt`               | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | `WorkspaceState::begin_deployment_attempt` 被 `redeploy_deployment_path_version`、`refresh_deployment_path`、`run_pilot_validation`、`take_over_deployment_path_routes_inner` 调用 | 追加 attempt，冻结线路快照并支撑失败重试               | `deployment_retries_append_attempts_and_freeze_the_path_snapshot`; `current_run_query_keeps_the_online_version_after_a_failed_update`             | `internalize` | 移除 IPC/command 属性；保留上列内部函数或 Workspace 数据行为，CodeGraph callers/impact 已用 `rg` 补证。                         |
| `bind_config_profile`                    | 无；仅 API 再导出/包装                                                                                                                                                                                                                                                            | `bindConfigProfile` in `apps/desktop/src/api/config-profiles.ts`            | no                | `WorkspaceState::bind_config_profile`（迁移/重连测试直接调用）                                                                                                                     | 旧 profile binding 的增量 schema 与项目重连            | `stores_reusable_profiles_and_project_bindings_without_secret_values`; `upgrades_profile_bindings_and_supports_multiple_profiles_per_environment` | `internalize` | 移除 IPC/command 属性；保留上列内部函数或 Workspace 数据行为，CodeGraph callers/impact 已用 `rg` 补证。                         |
| `bind_project_server`                    | `apps/desktop/src/features/deployment-editor/server-connection.ts`                                                                                                                                                                                                                | `bindProjectServer` in `apps/desktop/src/api.ts`                            | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | Deployment aggregate、线路、current pointer 或恢复事实 | server-deployment/DeploymentEditor Feature tests; `src-tauri/src/tests.rs`; Workspace tests                                                       | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `bootstrap_server_caddy`                 | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | route/server_deploy 共享行为测试，不要求旧 endpoint 存在                                                                                          | `delete`      | 旧 staging/production/路由管理 endpoint 无生产消费者；未列入 internalize 的实现没有当前 Rust caller 或数据职责。                |
| `cancel_local_preview_start`             | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | `src-tauri/src/tests.rs` 的 managed_local/local_preview 共享行为保护                                                                              | `delete`      | 旧本机准备/单服务控制无生产消费者；当前 `start_local_preview`、受管工作区与连续验证使用独立入口。                               |
| `check_cnb_repository_access`            | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | Workspace/runtime_config 保护测试；无独立 endpoint 存在性要求                                                                                     | `delete`      | 旧数据/配置/秘密/Provider 管理 endpoint 无生产消费者；未列入 internalize 的实现无当前主线或迁移职责。                           |
| `check_cnb_secret_repository_access`     | `apps/desktop/src/features/deployment-editor/server-deployment-authorization.ts`                                                                                                                                                                                                  | `checkCnbSecretRepositoryAccess` in `apps/desktop/src/api.ts`               | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | 连接/秘密引用；秘密正文保持 Keychain 边界              | server-deployment/DeploymentEditor Feature tests; `src-tauri/src/tests.rs`; Workspace tests                                                       | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `check_deployment_routes`                | `apps/desktop/src/features/deployment-editor/deployment-editor-services.ts`                                                                                                                                                                                                       | `checkDeploymentRoutes` in `apps/desktop/src/api.ts`                        | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | Deployment aggregate、线路、current pointer 或恢复事实 | server-deployment/DeploymentEditor Feature tests; `src-tauri/src/tests.rs`; Workspace tests                                                       | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `check_docker`                           | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | `src-tauri/src/tests.rs` 的 open/manifest 行为保护，不要求旧 endpoint 存在                                                                        | `delete`      | 无生产消费者，`rg` 仅命令定义/handler；共享 manifest/SSH/Workspace 内核由当前入口另行保留。                                     |
| `check_registry_credentials`             | 无 Tauri Feature consumer；仅 `replaceRegistryCredentials` 浏览器回退                                                                                                                                                                                                             | `checkRegistryCredentials` in `apps/desktop/src/api/secrets.ts`             | yes               | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | `apps/desktop/src/api.test.ts` 的旧包装/浏览器契约；无 Tauri Feature 行为保护                                                                     | `delete`      | bundle 字符串仅由 `replaceRegistryCredentials` 的浏览器回退保活；Tauri 分支无 Feature caller，改为纯浏览器校验后删除 endpoint。 |
| `check_saved_registry_credentials`       | `apps/desktop/src/features/deployment-editor/server-deployment-setup-service.ts`                                                                                                                                                                                                  | `checkSavedRegistryCredentials` in `apps/desktop/src/api/secrets.ts`        | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | 连接/秘密引用；秘密正文保持 Keychain 边界              | server-deployment/DeploymentEditor Feature tests; `src-tauri/src/tests.rs`; Workspace tests                                                       | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `check_server`                           | `apps/desktop/src/features/deployment-editor/server-connection.ts`                                                                                                                                                                                                                | `checkServer` in `apps/desktop/src/api.ts`                                  | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | 连接/秘密引用；秘密正文保持 Keychain 边界              | server-deployment/DeploymentEditor Feature tests; `src-tauri/src/tests.rs`; Workspace tests                                                       | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `connect_cnb`                            | `apps/desktop/src/features/deployment-editor/server-deployment-authorization.ts`                                                                                                                                                                                                  | `connectCnb` in `apps/desktop/src/api.ts`                                   | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | 连接/秘密引用；秘密正文保持 Keychain 边界              | server-deployment/DeploymentEditor Feature tests; `src-tauri/src/tests.rs`; Workspace tests                                                       | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `continue_existing_deployment`           | `apps/desktop/src/features/deployment-editor/deployment-editor-services.ts`                                                                                                                                                                                                       | `continueExistingDeployment` in `apps/desktop/src/api.ts`                   | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | Deployment aggregate、线路、current pointer 或恢复事实 | server-deployment/DeploymentEditor Feature tests; `src-tauri/src/tests.rs`; Workspace tests                                                       | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `create_cnb_repository`                  | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | Workspace/runtime_config 保护测试；无独立 endpoint 存在性要求                                                                                     | `delete`      | 旧数据/配置/秘密/Provider 管理 endpoint 无生产消费者；未列入 internalize 的实现无当前主线或迁移职责。                           |
| `create_deployment_task`                 | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | `mvp_environment::prepare_managed_server_deployment`、`pilot_validation::run_pilot_validation` 调用 `create_deployment_task_inner`                                                 | 创建固定输入的 deployment run 与线路绑定               | `runtime_dependency_failures_resume_the_same_server_deploy`; server deploy tests                                                                  | `internalize` | 移除 IPC/command 属性；保留上列内部函数或 Workspace 数据行为，CodeGraph callers/impact 已用 `rg` 补证。                         |
| `create_managed_local_run_workspace`     | `apps/desktop/src/features/deployment-editor/deployment-editor-services.ts`                                                                                                                                                                                                       | `createManagedLocalRunWorkspace` in `apps/desktop/src/api.ts`               | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | Deployment aggregate、线路、current pointer 或恢复事实 | `DeploymentEditorController.test.tsx`; `source_snapshots.rs` tests; `deployment-evidence/model.test.ts`                                           | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `delete_config_profile`                  | 无；仅 API 再导出/包装                                                                                                                                                                                                                                                            | `deleteConfigProfile` in `apps/desktop/src/api/config-profiles.ts`          | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | `apps/desktop/src/api.test.ts` 的旧包装/浏览器契约；无 Tauri Feature 行为保护                                                                     | `delete`      | 包装无 Feature caller并被生产 tree-shaking 移除；无独立 Rust caller 或必须保留的数据职责。                                      |
| `delete_deployment_path`                 | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | `WorkspaceState::delete_deployment_path`（事务保护测试直接调用）                                                                                                                   | 活动任务期间拒绝删除线路的事务约束                     | `deployment_paths_are_project_scoped_reusable_connection_bindings`                                                                                | `internalize` | 移除 IPC/command 属性；保留上列内部函数或 Workspace 数据行为，CodeGraph callers/impact 已用 `rg` 补证。                         |
| `delete_secret`                          | 无；仅 API 再导出/包装                                                                                                                                                                                                                                                            | `deleteSecret` in `apps/desktop/src/api/secrets.ts`                         | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | `apps/desktop/src/api.test.ts` 的旧包装/浏览器契约；无 Tauri Feature 行为保护                                                                     | `delete`      | 包装无 Feature caller并被生产 tree-shaking 移除；无独立 Rust caller 或必须保留的数据职责。                                      |
| `detect_dns_provider`                    | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | route/server_deploy 共享行为测试，不要求旧 endpoint 存在                                                                                          | `delete`      | 旧 staging/production/路由管理 endpoint 无生产消费者；未列入 internalize 的实现没有当前 Rust caller 或数据职责。                |
| `discover_ssh_identities`                | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | `src-tauri/src/tests.rs` 的 open/manifest 行为保护，不要求旧 endpoint 存在                                                                        | `delete`      | 无生产消费者，`rg` 仅命令定义/handler；共享 manifest/SSH/Workspace 内核由当前入口另行保留。                                     |
| `enable_cnb_auto_trigger`                | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | Workspace/runtime_config 保护测试；无独立 endpoint 存在性要求                                                                                     | `delete`      | 旧数据/配置/秘密/Provider 管理 endpoint 无生产消费者；未列入 internalize 的实现无当前主线或迁移职责。                           |
| `ensure_cnb_repository`                  | `apps/desktop/src/features/deployment-editor/server-deployment-authorization.ts`                                                                                                                                                                                                  | `ensureCnbRepository` in `apps/desktop/src/api.ts`                          | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | 连接/秘密引用；秘密正文保持 Keychain 边界              | server-deployment/DeploymentEditor Feature tests; `src-tauri/src/tests.rs`; Workspace tests                                                       | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `forget_project`                         | `apps/desktop/src/App.tsx`                                                                                                                                                                                                                                                        | `forgetProject` in `apps/desktop/src/api.ts`                                | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | Deployment aggregate、线路、current pointer 或恢复事实 | `apps/desktop/src/api.test.ts`; adjacent Feature tests; `src-tauri/src/tests.rs`                                                                  | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `generate_runtime_secret`                | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | Workspace/runtime_config 保护测试；无独立 endpoint 存在性要求                                                                                     | `delete`      | 旧数据/配置/秘密/Provider 管理 endpoint 无生产消费者；未列入 internalize 的实现无当前主线或迁移职责。                           |
| `generate_ssh_identity`                  | `apps/desktop/src/features/deployment-editor/server-connection.ts`                                                                                                                                                                                                                | `generateSshIdentity` in `apps/desktop/src/api.ts`                          | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | —                                                      | `apps/desktop/src/api.test.ts`; adjacent Feature tests; `src-tauri/src/tests.rs`                                                                  | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `get_app_setting`                        | `apps/desktop/src/features/deployment-editor/server-deployment-setup-service.ts`                                                                                                                                                                                                  | `getAppSetting` in `apps/desktop/src/api/settings.ts`                       | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | 应用非秘密设置                                         | `apps/desktop/src/api.test.ts`; adjacent Feature tests; `src-tauri/src/tests.rs`                                                                  | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `get_app_settings`                       | 无；仅 API 再导出/包装                                                                                                                                                                                                                                                            | `getAppSettings` in `apps/desktop/src/api/settings.ts`                      | no                | `reconcile_compat_connections` 调用 `WorkspaceState::settings`                                                                                                                     | 旧 Provider 设置到 Connection 的兼容回填               | `connection_resources_backfill_legacy_providers_without_exposing_secrets`                                                                         | `internalize` | 移除 IPC/command 属性；保留上列内部函数或 Workspace 数据行为，CodeGraph callers/impact 已用 `rg` 补证。                         |
| `get_cnb_account`                        | `apps/desktop/src/features/deployment-editor/server-deployment-authorization.ts`                                                                                                                                                                                                  | `getCnbAccount` in `apps/desktop/src/api.ts`                                | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | 连接/秘密引用；秘密正文保持 Keychain 边界              | server-deployment/DeploymentEditor Feature tests; `src-tauri/src/tests.rs`; Workspace tests                                                       | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `get_local_development_support`          | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | `src-tauri/src/tests.rs` 的 managed_local/local_preview 共享行为保护                                                                              | `delete`      | 旧本机准备/单服务控制无生产消费者；当前 `start_local_preview`、受管工作区与连续验证使用独立入口。                               |
| `get_local_infrastructure_status`        | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | `src-tauri/src/tests.rs` 的 managed_local/local_preview 共享行为保护                                                                              | `delete`      | 旧本机准备/单服务控制无生产消费者；当前 `start_local_preview`、受管工作区与连续验证使用独立入口。                               |
| `get_local_preview_status`               | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | `src-tauri/src/tests.rs` 的 managed_local/local_preview 共享行为保护                                                                              | `delete`      | 旧本机准备/单服务控制无生产消费者；当前 `start_local_preview`、受管工作区与连续验证使用独立入口。                               |
| `get_preflight`                          | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | `src-tauri/src/tests.rs` 的 open/manifest 行为保护，不要求旧 endpoint 存在                                                                        | `delete`      | 无生产消费者，`rg` 仅命令定义/handler；共享 manifest/SSH/Workspace 内核由当前入口另行保留。                                     |
| `get_project_connection_bindings`        | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | `WorkspaceState::project_connection_bindings`（兼容回填测试直接调用）                                                                                                              | 旧项目连接绑定回填且不读取秘密正文                     | `migrates_legacy_workspace_model_idempotently`; `relinks_a_moved_project_without_losing_history_bindings_or_settings`                             | `internalize` | 移除 IPC/command 属性；保留上列内部函数或 Workspace 数据行为，CodeGraph callers/impact 已用 `rg` 补证。                         |
| `get_project_server`                     | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | `capture_deployment_artifacts`、`verify_public_routes`、`deployment_server_profile` 调用 `WorkspaceState::server_for_project`                                                      | 旧 server binding 与当前部署/路由恢复兼容              | `migrates_legacy_workspace_model_idempotently`; `upgrades_existing_server_records_with_host_fingerprints`                                         | `internalize` | 移除 IPC/command 属性；保留上列内部函数或 Workspace 数据行为，CodeGraph callers/impact 已用 `rg` 补证。                         |
| `inspect_server_route_conflicts`         | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | route/server_deploy 共享行为测试，不要求旧 endpoint 存在                                                                                          | `delete`      | 旧 staging/production/路由管理 endpoint 无生产消费者；未列入 internalize 的实现没有当前 Rust caller 或数据职责。                |
| `install_server_key_with_password`       | `apps/desktop/src/features/deployment-editor/server-connection.ts`                                                                                                                                                                                                                | `installServerKeyWithPassword` in `apps/desktop/src/api.ts`                 | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | 连接/秘密引用；秘密正文保持 Keychain 边界              | server-deployment/DeploymentEditor Feature tests; `src-tauri/src/tests.rs`; Workspace tests                                                       | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `list_active_deployment_runs`            | `apps/desktop/src/features/deployment-list/use-deployment-dashboard.ts`                                                                                                                                                                                                           | `listActiveDeploymentRuns` in `apps/desktop/src/api.ts`                     | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | Deployment aggregate、线路、current pointer 或恢复事实 | `use-deployment-dashboard.test.ts`; `ProjectGallery.test.tsx`; Workspace tests                                                                    | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `list_attention_deployment_runs`         | `apps/desktop/src/features/deployment-list/use-deployment-dashboard.ts`                                                                                                                                                                                                           | `listAttentionDeploymentRuns` in `apps/desktop/src/api.ts`                  | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | Deployment aggregate、线路、current pointer 或恢复事实 | `use-deployment-dashboard.test.ts`; `ProjectGallery.test.tsx`; Workspace tests                                                                    | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `list_config_profile_bindings`           | 无；仅 API 再导出/包装                                                                                                                                                                                                                                                            | `listConfigProfileBindings` in `apps/desktop/src/api/config-profiles.ts`    | no                | `WorkspaceState::config_profile_bindings`（迁移/重连测试直接调用）                                                                                                                 | 旧 profile binding 迁移及重启读取                      | `upgrades_profile_bindings_and_supports_multiple_profiles_per_environment`                                                                        | `internalize` | 移除 IPC/command 属性；保留上列内部函数或 Workspace 数据行为，CodeGraph callers/impact 已用 `rg` 补证。                         |
| `list_config_profiles`                   | 无；仅 API 再导出/包装                                                                                                                                                                                                                                                            | `listConfigProfiles` in `apps/desktop/src/api/config-profiles.ts`           | no                | `WorkspaceState::list_config_profiles`（配置迁移测试读取）                                                                                                                         | Config Profile 旧数据读取；本机基础设施仍保存 profile  | `stores_reusable_profiles_and_project_bindings_without_secret_values`                                                                             | `internalize` | 移除 IPC/command 属性；保留上列内部函数或 Workspace 数据行为，CodeGraph callers/impact 已用 `rg` 补证。                         |
| `list_connections`                       | `apps/desktop/src/features/deployment-editor/server-deployment-setup-service.ts`                                                                                                                                                                                                  | `listConnections` in `apps/desktop/src/api.ts`                              | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | 连接/秘密引用；秘密正文保持 Keychain 边界              | server-deployment/DeploymentEditor Feature tests; `src-tauri/src/tests.rs`; Workspace tests                                                       | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `list_current_deployment_runs`           | `apps/desktop/src/features/deployment-list/use-deployment-dashboard.ts`                                                                                                                                                                                                           | `listCurrentDeploymentRuns` in `apps/desktop/src/api.ts`                    | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | Deployment aggregate、线路、current pointer 或恢复事实 | `use-deployment-dashboard.test.ts`; `ProjectGallery.test.tsx`; Workspace tests                                                                    | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `list_deployment_attempts`               | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | `WorkspaceState::list_deployment_attempts`（attempt 事务测试读取）                                                                                                                 | attempt 追加历史与重试审计                             | `deployment_retries_append_attempts_and_freeze_the_path_snapshot`                                                                                 | `internalize` | 移除 IPC/command 属性；保留上列内部函数或 Workspace 数据行为，CodeGraph callers/impact 已用 `rg` 补证。                         |
| `list_deployment_path_runs`              | `apps/desktop/src/features/deployment-detail/deployment-detail-services.ts`                                                                                                                                                                                                       | `listDeploymentPathRuns` in `apps/desktop/src/api.ts`                       | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | Deployment aggregate、线路、current pointer 或恢复事实 | `deployment-detail/model.test.ts`; `ProjectGallery.test.tsx`; Workspace/Tauri tests                                                               | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `list_deployment_paths`                  | `apps/desktop/src/App.tsx`<br>`apps/desktop/src/features/deployment-detail/deployment-detail-services.ts`<br>`apps/desktop/src/features/deployment-editor/deployment-editor-services.ts`<br>`apps/desktop/src/features/deployment-editor/server-deployment-setup-service.ts`      | `listDeploymentPaths` in `apps/desktop/src/api.ts`                          | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | Deployment aggregate、线路、current pointer 或恢复事实 | `deployment-detail/model.test.ts`; `ProjectGallery.test.tsx`; Workspace/Tauri tests                                                               | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `list_deployment_runs`                   | `apps/desktop/src/features/deployment-editor/deployment-editor-services.ts`                                                                                                                                                                                                       | `listDeploymentRuns` in `apps/desktop/src/api.ts`                           | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | Deployment aggregate、线路、current pointer 或恢复事实 | server-deployment/DeploymentEditor Feature tests; `src-tauri/src/tests.rs`; Workspace tests                                                       | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `list_project_environments`              | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | `WorkspaceState::list_project_environments`（当前指针/迁移测试读取）                                                                                                               | 旧环境表、current version/run 指针与失败不覆盖在线版本 | `migrates_legacy_failed_only_project_without_inventing_a_version`; `newer_failed_or_needs_action_production_does_not_replace_online_version`      | `internalize` | 移除 IPC/command 属性；保留上列内部函数或 Workspace 数据行为，CodeGraph callers/impact 已用 `rg` 补证。                         |
| `list_project_versions`                  | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | `WorkspaceState::list_project_versions`（版本回填/恢复测试读取）                                                                                                                   | 版本/制品回填、恢复历史与不可变摘要                    | `restart_backfills_production_history_and_preserves_a_newer_rollback`; `production_digest_mismatch_is_atomic_and_does_not_switch_pointer`         | `internalize` | 移除 IPC/command 属性；保留上列内部函数或 Workspace 数据行为，CodeGraph callers/impact 已用 `rg` 补证。                         |
| `list_recent_projects`                   | `apps/desktop/src/features/deployment-list/use-deployment-dashboard.ts`                                                                                                                                                                                                           | `listRecentProjects` in `apps/desktop/src/api.ts`                           | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | Deployment aggregate、线路、current pointer 或恢复事实 | `use-deployment-dashboard.test.ts`; `ProjectGallery.test.tsx`; Workspace tests                                                                    | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `list_recent_successful_deployment_runs` | `apps/desktop/src/features/deployment-list/use-deployment-dashboard.ts`                                                                                                                                                                                                           | `listRecentSuccessfulDeploymentRuns` in `apps/desktop/src/api.ts`           | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | Deployment aggregate、线路、current pointer 或恢复事实 | `use-deployment-dashboard.test.ts`; `ProjectGallery.test.tsx`; Workspace tests                                                                    | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `list_servers`                           | `apps/desktop/src/features/deployment-editor/deployment-editor-services.ts`<br>`apps/desktop/src/features/deployment-editor/server-deployment-authorization.ts`<br>`apps/desktop/src/features/deployment-editor/server-deployment-setup-service.ts`                               | `listServers` in `apps/desktop/src/api.ts`                                  | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | 连接/秘密引用；秘密正文保持 Keychain 边界              | server-deployment/DeploymentEditor Feature tests; `src-tauri/src/tests.rs`; Workspace tests                                                       | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `list_version_validations`               | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | `WorkspaceState::list_version_validations`（重启验证测试读取）                                                                                                                     | 版本验证跨重启持久化                                   | `persists_version_validation_across_workspace_restart`                                                                                            | `internalize` | 移除 IPC/command 属性；保留上列内部函数或 Workspace 数据行为，CodeGraph callers/impact 已用 `rg` 补证。                         |
| `load_existing_project_config`           | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | Workspace/runtime_config 保护测试；无独立 endpoint 存在性要求                                                                                     | `delete`      | 旧数据/配置/秘密/Provider 管理 endpoint 无生产消费者；未列入 internalize 的实现无当前主线或迁移职责。                           |
| `load_runtime_config`                    | `apps/desktop/src/features/deployment-editor/deployment-editor-services.ts`<br>`apps/desktop/src/features/deployment-editor/use-runtime-configuration-recovery.ts`                                                                                                                | `loadRuntimeConfig` in `apps/desktop/src/api.ts`                            | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | Deployment aggregate、线路、current pointer 或恢复事实 | server-deployment/DeploymentEditor Feature tests; `src-tauri/src/tests.rs`; Workspace tests                                                       | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `open_project`                           | `apps/desktop/src/App.tsx`<br>`apps/desktop/src/features/deployment-editor/deployment-editor-services.ts`<br>`apps/desktop/src/features/deployment-editor/server-deployment-authorization.ts`<br>`apps/desktop/src/features/deployment-editor/server-deployment-setup-service.ts` | `openProject` in `apps/desktop/src/api.ts`                                  | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | Deployment aggregate、线路、current pointer 或恢复事实 | `apps/desktop/src/api.test.ts`; adjacent Feature tests; `src-tauri/src/tests.rs`                                                                  | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `open_staging_preview_tunnel`            | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | route/server_deploy 共享行为测试，不要求旧 endpoint 存在                                                                                          | `delete`      | 旧 staging/production/路由管理 endpoint 无生产消费者；未列入 internalize 的实现没有当前 Rust caller 或数据职责。                |
| `pause_deployment_task`                  | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | route/server_deploy 共享行为测试，不要求旧 endpoint 存在                                                                                          | `delete`      | 旧 staging/production/路由管理 endpoint 无生产消费者；未列入 internalize 的实现没有当前 Rust caller 或数据职责。                |
| `prepare_cnb_secret_bundle`              | `apps/desktop/src/features/deployment-editor/server-deployment-authorization.ts`                                                                                                                                                                                                  | `prepareCnbSecretBundle` in `apps/desktop/src/api.ts`                       | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | 连接/秘密引用；秘密正文保持 Keychain 边界              | server-deployment/DeploymentEditor Feature tests; `src-tauri/src/tests.rs`; Workspace tests                                                       | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `prepare_deployment_path_retry`          | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | `pilot_validation::run_pilot_validation` 调用 `prepare_deployment_path_retry_inner`                                                                                                | 按 server/routes 恢复点续跑且保留制品                  | `repaired_deployment_path_routes_keep_existing_artifacts_on_the_server`                                                                           | `internalize` | 移除 IPC/command 属性；保留上列内部函数或 Workspace 数据行为，CodeGraph callers/impact 已用 `rg` 补证。                         |
| `prepare_local_development`              | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | `src-tauri/src/tests.rs` 的 managed_local/local_preview 共享行为保护                                                                              | `delete`      | 旧本机准备/单服务控制无生产消费者；当前 `start_local_preview`、受管工作区与连续验证使用独立入口。                               |
| `prepare_local_infrastructure`           | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | `src-tauri/src/tests.rs` 的 managed_local/local_preview 共享行为保护                                                                              | `delete`      | 旧本机准备/单服务控制无生产消费者；当前 `start_local_preview`、受管工作区与连续验证使用独立入口。                               |
| `prepare_local_preview`                  | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | `src-tauri/src/tests.rs` 的 managed_local/local_preview 共享行为保护                                                                              | `delete`      | 旧本机准备/单服务控制无生产消费者；当前 `start_local_preview`、受管工作区与连续验证使用独立入口。                               |
| `prepare_managed_server_deployment`      | `apps/desktop/src/features/deployment-editor/deployment-editor-services.ts`                                                                                                                                                                                                       | `prepareManagedServerDeployment` in `apps/desktop/src/api.ts`               | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | Deployment aggregate、线路、current pointer 或恢复事实 | server-deployment/DeploymentEditor Feature tests; `src-tauri/src/tests.rs`; Workspace tests                                                       | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `prepare_managed_server_environment`     | `apps/desktop/src/features/deployment-editor/deployment-editor-services.ts`                                                                                                                                                                                                       | `prepareManagedServerEnvironment` in `apps/desktop/src/api.ts`              | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | 连接/秘密引用；秘密正文保持 Keychain 边界              | server-deployment/DeploymentEditor Feature tests; `src-tauri/src/tests.rs`; Workspace tests                                                       | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `prepare_pipeline_identity`              | `apps/desktop/src/features/deployment-editor/server-deployment-authorization.ts`                                                                                                                                                                                                  | `preparePipelineIdentity` in `apps/desktop/src/api.ts`                      | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | —                                                      | `apps/desktop/src/api.test.ts`; adjacent Feature tests; `src-tauri/src/tests.rs`                                                                  | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `preview_manifest`                       | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | `src-tauri/src/tests.rs` 的 open/manifest 行为保护，不要求旧 endpoint 存在                                                                        | `delete`      | 无生产消费者，`rg` 仅命令定义/handler；共享 manifest/SSH/Workspace 内核由当前入口另行保留。                                     |
| `project_managed_deployment_evidence`    | `apps/desktop/src/features/deployment-editor/deployment-editor-services.ts`                                                                                                                                                                                                       | `projectManagedDeploymentEvidence` in `apps/desktop/src/api.ts`             | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | Deployment aggregate、线路、current pointer 或恢复事实 | server-deployment/DeploymentEditor Feature tests; `src-tauri/src/tests.rs`; Workspace tests                                                       | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `promote_production_deployment`          | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | route/server_deploy 共享行为测试，不要求旧 endpoint 存在                                                                                          | `delete`      | 旧 staging/production/路由管理 endpoint 无生产消费者；未列入 internalize 的实现没有当前 Rust caller 或数据职责。                |
| `reapply_deployment_routes`              | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | route/server_deploy 共享行为测试，不要求旧 endpoint 存在                                                                                          | `delete`      | 旧 staging/production/路由管理 endpoint 无生产消费者；未列入 internalize 的实现没有当前 Rust caller 或数据职责。                |
| `recommend_runtime_config`               | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | `src-tauri/src/tests.rs` 的 managed_local/local_preview 共享行为保护                                                                              | `delete`      | 旧本机准备/单服务控制无生产消费者；当前 `start_local_preview`、受管工作区与连续验证使用独立入口。                               |
| `redeploy_deployment_path_version`       | `apps/desktop/src/features/deployment-detail/deployment-detail-services.ts`                                                                                                                                                                                                       | `redeployDeploymentPathVersion` in `apps/desktop/src/api.ts`                | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | Deployment aggregate、线路、current pointer 或恢复事实 | `deployment-detail/model.test.ts`; `ProjectGallery.test.tsx`; Workspace/Tauri tests                                                               | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `refresh_deployment`                     | `apps/desktop/src/features/deployment-editor/deployment-editor-services.ts`<br>`apps/desktop/src/features/deployment-list/use-deployment-dashboard.ts`                                                                                                                            | `refreshDeployment` in `apps/desktop/src/api.ts`                            | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | Deployment aggregate、线路、current pointer 或恢复事实 | `use-deployment-dashboard.test.ts`; `ProjectGallery.test.tsx`; Workspace tests                                                                    | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `relink_project`                         | `apps/desktop/src/App.tsx`                                                                                                                                                                                                                                                        | `relinkProject` in `apps/desktop/src/api.ts`                                | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | Deployment aggregate、线路、current pointer 或恢复事实 | `apps/desktop/src/api.test.ts`; adjacent Feature tests; `src-tauri/src/tests.rs`                                                                  | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `replace_registry_credentials`           | `apps/desktop/src/features/deployment-editor/server-deployment-authorization.ts`                                                                                                                                                                                                  | `replaceRegistryCredentials` in `apps/desktop/src/api/secrets.ts`           | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | 连接/秘密引用；秘密正文保持 Keychain 边界              | server-deployment/DeploymentEditor Feature tests; `src-tauri/src/tests.rs`; Workspace tests                                                       | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `reset_project_deployment`               | `apps/desktop/src/features/deployment-editor/deployment-editor-services.ts`<br>`apps/desktop/src/features/deployment-editor/use-deployment-adoption.ts`                                                                                                                           | `resetProjectDeployment` in `apps/desktop/src/api.ts`                       | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | Deployment aggregate、线路、current pointer 或恢复事实 | server-deployment/DeploymentEditor Feature tests; `src-tauri/src/tests.rs`; Workspace tests                                                       | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `resolve_local_folder_source`            | `apps/desktop/src/features/deployment-editor/deployment-editor-services.ts`                                                                                                                                                                                                       | `resolveLocalFolderSource` in `apps/desktop/src/api.ts`                     | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | 不可变源码/本机运行身份与证据                          | `DeploymentEditorController.test.tsx`; `source_snapshots.rs` tests; `deployment-evidence/model.test.ts`                                           | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `resolve_managed_server_environment`     | `apps/desktop/src/features/deployment-editor/deployment-editor-services.ts`                                                                                                                                                                                                       | `resolveManagedServerEnvironment` in `apps/desktop/src/api.ts`              | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | 连接/秘密引用；秘密正文保持 Keychain 边界              | server-deployment/DeploymentEditor Feature tests; `src-tauri/src/tests.rs`; Workspace tests                                                       | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `resume_staging_deployment`              | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | route/server_deploy 共享行为测试，不要求旧 endpoint 存在                                                                                          | `delete`      | 旧 staging/production/路由管理 endpoint 无生产消费者；未列入 internalize 的实现没有当前 Rust caller 或数据职责。                |
| `retry_deployment_certificates`          | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | route/server_deploy 共享行为测试，不要求旧 endpoint 存在                                                                                          | `delete`      | 旧 staging/production/路由管理 endpoint 无生产消费者；未列入 internalize 的实现没有当前 Rust caller 或数据职责。                |
| `rollback_environment`                   | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | route/server_deploy 共享行为测试，不要求旧 endpoint 存在                                                                                          | `delete`      | 旧 staging/production/路由管理 endpoint 无生产消费者；未列入 internalize 的实现没有当前 Rust caller 或数据职责。                |
| `runtime_config_sync_status`             | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | Workspace/runtime_config 保护测试；无独立 endpoint 存在性要求                                                                                     | `delete`      | 旧数据/配置/秘密/Provider 管理 endpoint 无生产消费者；未列入 internalize 的实现无当前主线或迁移职责。                           |
| `runtime_secret_status`                  | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | Workspace/runtime_config 保护测试；无独立 endpoint 存在性要求                                                                                     | `delete`      | 旧数据/配置/秘密/Provider 管理 endpoint 无生产消费者；未列入 internalize 的实现无当前主线或迁移职责。                           |
| `save_config_profile`                    | 无；仅 API 再导出/包装                                                                                                                                                                                                                                                            | `saveConfigProfile` in `apps/desktop/src/api/config-profiles.ts`            | no                | `local_runtime::save_local_infrastructure_profiles` 调用 `WorkspaceState::save_config_profile`                                                                                     | 本机 PostgreSQL/Redis profile 持久化，不保存秘密正文   | `stores_reusable_profiles_and_project_bindings_without_secret_values`; local infrastructure tests                                                 | `internalize` | 移除 IPC/command 属性；保留上列内部函数或 Workspace 数据行为，CodeGraph callers/impact 已用 `rg` 补证。                         |
| `save_deployment_path`                   | `apps/desktop/src/features/deployment-editor/server-deployment-setup-service.ts`                                                                                                                                                                                                  | `saveDeploymentPath` in `apps/desktop/src/api.ts`                           | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | Deployment aggregate、线路、current pointer 或恢复事实 | server-deployment/DeploymentEditor Feature tests; `src-tauri/src/tests.rs`; Workspace tests                                                       | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `save_manifest_draft`                    | `apps/desktop/src/features/deployment-editor/server-deployment-authorization.ts`                                                                                                                                                                                                  | `saveManifestDraft` in `apps/desktop/src/api.ts`                            | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | —                                                      | server-deployment/DeploymentEditor Feature tests; `src-tauri/src/tests.rs`; Workspace tests                                                       | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `save_project_step`                      | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | `pilot_validation::run_pilot_validation` 调用 `WorkspaceState::set_project_step`                                                                                                   | 旧项目 adoption/恢复步骤兼容                           | open/adoption tests; `run_pilot_validation` path                                                                                                  | `internalize` | 移除 IPC/command 属性；保留上列内部函数或 Workspace 数据行为，CodeGraph callers/impact 已用 `rg` 补证。                         |
| `secret_status`                          | 无；仅 API 再导出/包装                                                                                                                                                                                                                                                            | `getSecretStatus` in `apps/desktop/src/api/secrets.ts`                      | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | `apps/desktop/src/api.test.ts` 的旧包装/浏览器契约；无 Tauri Feature 行为保护                                                                     | `delete`      | 包装无 Feature caller并被生产 tree-shaking 移除；无独立 Rust caller 或必须保留的数据职责。                                      |
| `set_app_setting`                        | `apps/desktop/src/features/deployment-editor/server-deployment-authorization.ts`                                                                                                                                                                                                  | `setAppSetting` in `apps/desktop/src/api/settings.ts`                       | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | 应用非秘密设置                                         | `apps/desktop/src/api.test.ts`; adjacent Feature tests; `src-tauri/src/tests.rs`                                                                  | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `set_environment_config_bindings`        | 无；仅 API 再导出/包装                                                                                                                                                                                                                                                            | `setEnvironmentConfigBindings` in `apps/desktop/src/api/config-profiles.ts` | no                | `WorkspaceState::set_environment_config_bindings`（兼容 schema 测试直接调用）                                                                                                      | 旧 profile binding schema 幂等迁移                     | `upgrades_profile_bindings_and_supports_multiple_profiles_per_environment`                                                                        | `internalize` | 移除 IPC/command 属性；保留上列内部函数或 Workspace 数据行为，CodeGraph callers/impact 已用 `rg` 补证。                         |
| `set_local_infrastructure_service`       | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | `src-tauri/src/tests.rs` 的 managed_local/local_preview 共享行为保护                                                                              | `delete`      | 旧本机准备/单服务控制无生产消费者；当前 `start_local_preview`、受管工作区与连续验证使用独立入口。                               |
| `set_version_validation`                 | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | `WorkspaceState::set_version_validation`（版本验证事务测试直接调用）                                                                                                               | 版本验证原子写入并关联 immutable version               | `persists_version_validation_across_workspace_restart`                                                                                            | `internalize` | 移除 IPC/command 属性；保留上列内部函数或 Workspace 数据行为，CodeGraph callers/impact 已用 `rg` 补证。                         |
| `start_deployment_path`                  | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | `pilot_validation::run_pilot_validation` 调用 `start_deployment_path_inner`                                                                                                        | 当前 pilot 的 build/registry/deploy 恢复链             | server deploy tests; `run_pilot_validation` path                                                                                                  | `internalize` | 移除 IPC/command 属性；保留上列内部函数或 Workspace 数据行为，CodeGraph callers/impact 已用 `rg` 补证。                         |
| `start_local_preview`                    | `apps/desktop/src/features/deployment-editor/deployment-editor-services.ts`                                                                                                                                                                                                       | `startLocalPreview` in `apps/desktop/src/api.ts`                            | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | 不可变源码/本机运行身份与证据                          | `DeploymentEditorController.test.tsx`; `source_snapshots.rs` tests; `deployment-evidence/model.test.ts`                                           | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `start_local_preview_service`            | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | `src-tauri/src/tests.rs` 的 managed_local/local_preview 共享行为保护                                                                              | `delete`      | 旧本机准备/单服务控制无生产消费者；当前 `start_local_preview`、受管工作区与连续验证使用独立入口。                               |
| `start_staging_deployment`               | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | route/server_deploy 共享行为测试，不要求旧 endpoint 存在                                                                                          | `delete`      | 旧 staging/production/路由管理 endpoint 无生产消费者；未列入 internalize 的实现没有当前 Rust caller 或数据职责。                |
| `stop_local_preview`                     | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | `src-tauri/src/tests.rs` 的 managed_local/local_preview 共享行为保护                                                                              | `delete`      | 旧本机准备/单服务控制无生产消费者；当前 `start_local_preview`、受管工作区与连续验证使用独立入口。                               |
| `stop_local_preview_service`             | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | `src-tauri/src/tests.rs` 的 managed_local/local_preview 共享行为保护                                                                              | `delete`      | 旧本机准备/单服务控制无生产消费者；当前 `start_local_preview`、受管工作区与连续验证使用独立入口。                               |
| `stop_managed_local_port_owner`          | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | `src-tauri/src/tests.rs` 的 managed_local/local_preview 共享行为保护                                                                              | `delete`      | 旧本机准备/单服务控制无生产消费者；当前 `start_local_preview`、受管工作区与连续验证使用独立入口。                               |
| `store_runtime_config`                   | `apps/desktop/src/features/deployment-editor/deployment-editor-services.ts`<br>`apps/desktop/src/features/deployment-editor/use-runtime-configuration-recovery.ts`                                                                                                                | `storeRuntimeConfig` in `apps/desktop/src/api.ts`                           | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | Deployment aggregate、线路、current pointer 或恢复事实 | server-deployment/DeploymentEditor Feature tests; `src-tauri/src/tests.rs`; Workspace tests                                                       | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `store_runtime_secret`                   | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | Workspace/runtime_config 保护测试；无独立 endpoint 存在性要求                                                                                     | `delete`      | 旧数据/配置/秘密/Provider 管理 endpoint 无生产消费者；未列入 internalize 的实现无当前主线或迁移职责。                           |
| `store_secret`                           | 无；仅 API 再导出/包装                                                                                                                                                                                                                                                            | `storeSecret` in `apps/desktop/src/api/secrets.ts`                          | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | `apps/desktop/src/api.test.ts` 的旧包装/浏览器契约；无 Tauri Feature 行为保护                                                                     | `delete`      | 包装无 Feature caller并被生产 tree-shaking 移除；无独立 Rust caller 或必须保留的数据职责。                                      |
| `sync_external_deployments`              | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | route/server_deploy 共享行为测试，不要求旧 endpoint 存在                                                                                          | `delete`      | 旧 staging/production/路由管理 endpoint 无生产消费者；未列入 internalize 的实现没有当前 Rust caller 或数据职责。                |
| `sync_project_to_cnb`                    | `apps/desktop/src/features/deployment-editor/deployment-editor-services.ts`                                                                                                                                                                                                       | `syncProjectToCnb` in `apps/desktop/src/api.ts`                             | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | Deployment aggregate、线路、current pointer 或恢复事实 | server-deployment/DeploymentEditor Feature tests; `src-tauri/src/tests.rs`; Workspace tests                                                       | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `sync_runtime_config_to_server`          | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | Workspace/runtime_config 保护测试；无独立 endpoint 存在性要求                                                                                     | `delete`      | 旧数据/配置/秘密/Provider 管理 endpoint 无生产消费者；未列入 internalize 的实现无当前主线或迁移职责。                           |
| `take_over_deployment_path_routes`       | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | `pilot_validation::run_pilot_validation` 调用 `take_over_deployment_path_routes_inner`                                                                                             | 路由接管后只续验证并保留既有制品                       | `persists_per_address_route_checks_across_workspace_restart`; `repaired_deployment_path_routes_keep_existing_artifacts_on_the_server`             | `internalize` | 移除 IPC/command 属性；保留上列内部函数或 Workspace 数据行为，CodeGraph callers/impact 已用 `rg` 补证。                         |
| `take_over_server_routes`                | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | `take_over_deployment_path_routes_inner` 复用 `take_over_caddy_routes`                                                                                                             | 共享 Caddy 原子接管、校验与恢复实现                    | route takeover/rollback tests; `repaired_deployment_path_routes_keep_existing_artifacts_on_the_server`                                            | `internalize` | 移除 IPC/command 属性；保留上列内部函数或 Workspace 数据行为，CodeGraph callers/impact 已用 `rg` 补证。                         |
| `verify_managed_local_run`               | `apps/desktop/src/features/deployment-editor/deployment-editor-services.ts`                                                                                                                                                                                                       | `verifyManagedLocalRun` in `apps/desktop/src/api.ts`                        | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | Deployment aggregate、线路、current pointer 或恢复事实 | `DeploymentEditorController.test.tsx`; `source_snapshots.rs` tests; `deployment-evidence/model.test.ts`                                           | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `write_local_env`                        | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | `src-tauri/src/tests.rs` 的 managed_local/local_preview 共享行为保护                                                                              | `delete`      | 旧本机准备/单服务控制无生产消费者；当前 `start_local_preview`、受管工作区与连续验证使用独立入口。                               |
