# 桌面命令面逐命令证据矩阵

## 审计基线

- 当前随应用打包的前端是唯一受支持的 IPC 消费者；没有外部 SDK、插件或远程 IPC 兼容承诺。
- Comet 状态 `base_ref`: `089cdb7470a2f208a179336993fab61791b4bf40`；实施计划 front matter `base-ref`: `24e0fc79f9bda0401362b4e93cb570774b4abfa8`。两者之间只有规划/状态资产，生产命令面未变化；本次实际审计起点 HEAD 为 `77ff7066c945bf204bc56c1783e5e4b246a39dbd`。
- 实际命令：`pnpm --filter @abcdeploy/desktop build`（PASS，仅既有 Vite chunk-size warning）；`node scripts/check-desktop-command-surface.mjs --mode all`（按设计退出 1）；`node scripts/check-desktop-command-surface.mjs --mode all --json`（同一结果对象）。
- 数量：`registered=111`、`source=56`、`bundled=46`、`dynamicInvocations=0`；差异为 `missingRegistrations=0`、`registeredOnly=55`、`bundleOnly=0`、`sourceNotBundled=10`。
- 65 个未进入 bundle 的候选 = 55 个 `registeredOnly` + 10 个 `sourceNotBundled`，均在下表各占一行。另将 `check_registry_credentials` 单独裁决：它在 bundle 中，但字符串只由浏览器回退路径保活，不是 Tauri Feature 消费证据。
- 证据方法：`pnpm codegraph:status` 确认索引最新；对每个 `internalize` 候选运行 `codegraph callers`、`codegraph impact`，并用 `rg -n <symbol> apps/desktop/src-tauri/src` 复核 Tauri 动态注册、同名 Workspace 方法和嵌套 caller。CodeGraph 报告 handler “无 caller”时，以表中明确列出的 `*_inner`、Workspace caller 或保护测试补证。

## Task 3 实施记录（2026-08-02）

- 已删除根 API 的 11 个无生产消费者包装：`listConfigProfiles`、`saveConfigProfile`、`deleteConfigProfile`、`bindConfigProfile`、`listConfigProfileBindings`、`setEnvironmentConfigBindings`、`getAppSettings`、`getSecretStatus`、`storeSecret`、`deleteSecret`、`checkRegistryCredentials`；`apps/desktop/src/api/config-profiles.ts` 整体删除。保留签名为 `replaceRegistryCredentials`、`checkSavedRegistryCredentials`、`getAppSetting`、`setAppSetting`。
- `replaceRegistryCredentials` 的浏览器分支改用私有纯函数 `validateRegistryCredentials`；空密码返回 `AD-IMG-201` 且不写 verified 时间，完整输入只写 ISO 时间，不保存秘密。`check_registry_credentials` 已从 Rust import、`generate_handler!` 和 `credentials.rs` 删除。
- 已移除 `@radix-ui/react-collapsible`、`@radix-ui/react-dropdown-menu`、`@tauri-apps/plugin-clipboard-manager`、`tauri-plugin-clipboard-manager`、clipboard plugin 初始化和 `clipboard-manager:allow-write-text`。生产仍直接使用 `navigator.clipboard.writeText`，并保留有生产调用者的 dialog `open` / opener `openUrl` 及对应依赖、runtime plugin 和权限。
- RED：`pnpm --filter @abcdeploy/desktop test -- src/api.test.ts` 失败并完整列出 11 个待退役导出；GREEN 与目标回归为 36 files / 195 tests PASS，严格 TypeScript unused 检查和 desktop build PASS（仅既有 Vite chunk-size warning）。
- `source`、`bundle`、`all` 三种审计模式均按预期 exit 1，仅剩 65 个 `registeredOnly`；实际计数为 `registered=110`、`source=45`、`bundled=45`，且 `missingRegistrations=0`、`bundleOnly=0`、`sourceNotBundled=0`。

## Task 4 实施记录（2026-08-02）

- 已从 `apps/desktop/src-tauri/src/lib.rs` 删除 `get_preflight`、`save_project_step`、`preview_manifest`、`apply_manifest`、`check_docker`、`discover_ssh_identities` 的 Tauri adapter 与 `generate_handler!` 注册；仅由旧 adapter 使用的 `SystemPreflight`、`system_preflight` 和 `docker` 导入同步删除。
- 保留 `open_project`、`continue_existing_deployment`、`reset_project_deployment`、`resolve_local_folder_source`、`save_manifest_draft`、`generate_ssh_identity`，以及 `WorkspaceState::set_project_step` 和 `pilot_validation::run_pilot_validation` 的内部职责；未修改这些接口的参数、返回值或错误字符串。
- RED：`source` 审计为 `registered/source=110/45`，六个目标命令均位于 65 个 `registeredOnly`；GREEN：三种审计均按预期 exit 1，实际计数为 `registered/source/bundled=104/45/45`，仅剩 59 个 `registeredOnly`，其余差异类别与 `dynamicInvocations` 均为 0。
- 删除前后 `cargo test -p abcdeploy-desktop manifest -- --nocapture` 均为 4 项 PASS；adoption continue/reset 保护测试与 deploy-core Manifest 注入防护测试 PASS。计划中的 `open_project` 过滤器命令 PASS，但当前测试名没有匹配项（0 tests），因此另以 adoption 测试、只读源码路径审计和编译/Clippy 门禁保护。

## Task 5 实施记录（2026-08-02）

- 已从 `apps/desktop/src-tauri/src/lib.rs` 删除 14 个旧本机 adapter、command 属性与 `generate_handler!` 注册；`rg` 与同步后的 CodeGraph 均无这些 endpoint 定义残留。仅由旧 endpoint 保活的本机基础设施状态/Compose/profile 写入、配置推荐、用户目录 `.env` 写入、plan 状态投影和单服务启停实现同步删除。
- 保留 `start_local_preview`、`create_managed_local_run_workspace`、`verify_managed_local_run`，以及当前入口仍调用的 `LocalStartTask`、共享进程表、进程组取消/超时终止、应用退出清理、端口 owner 识别、loopback URL、Compose/开发运行时与 `local_infrastructure_secret` 内核；三个 IPC 的参数、返回值、错误映射与注册均未改动。
- RED：source 审计为 `registered/source=104/45`、59 个 `registeredOnly`；取消测试改为内部 `cancel_local_start` 接缝后先因函数缺失编译失败。GREEN：取消/超时与 `local_` 过滤测试 10 项 PASS，`source_snapshots::tests` 9 项 PASS，Rust workspace 为 desktop 143 + core 109 + integration 3 项 PASS。计划中的 `managed_local` / `local_preview` 过滤器前后均 PASS 但当前测试名没有匹配项（0 tests）。
- 三种审计模式均按预期 exit 1，精确计数为 `registered/source/bundled=90/45/45`、45 个 `registeredOnly`；`missingRegistrations`、`bundleOnly`、`sourceNotBundled`、`dynamicInvocations` 均为 0。`cargo fmt --check`、workspace all-target Clippy `-D warnings`、desktop build、desktop production bundle、项目/秘密/diff 门禁及 CodeGraph sync/status 全部 PASS。

## Task 6 实施记录（2026-08-02）

- 已从 Tauri 命令面移除 19 个旧部署控制入口：`create_deployment_task`、`begin_deployment_attempt`、`list_deployment_attempts`、`pause_deployment_task`、`prepare_deployment_path_retry`、`start_staging_deployment`、`start_deployment_path`、`resume_staging_deployment`、`promote_production_deployment`、`retry_deployment_certificates`、`open_staging_preview_tunnel`、`sync_external_deployments`、`bootstrap_server_caddy`、`inspect_server_route_conflicts`、`take_over_server_routes`、`take_over_deployment_path_routes`、`reapply_deployment_routes`、`detect_dns_provider`、`rollback_environment`；对应 command 属性、adapter 和 `generate_handler!` 注册均已删除。
- 内部化保留 `create_deployment_task_inner`、`WorkspaceState::begin_deployment_attempt`、`WorkspaceState::list_deployment_attempts`、`prepare_deployment_path_retry_inner`、`start_deployment_path_inner`、`take_over_deployment_path_routes_inner` 与 `take_over_caddy_routes`。`list_deployment_attempts` 作为 attempt 审计读取保留，并用带原因的窄化 `dead_code` 许可表达暂时没有生产 caller；没有对共享执行器、状态转换或错误脱敏增加宽泛许可。
- 删除型入口的专属实现已一并收口：旧 staging/promote/rollback、证书重试、预览 tunnel、外部部署历史导入、Caddy bootstrap/冲突检查/整站重应用，以及 endpoint-only DNS provider 映射和类型均不再保留。当前 `prepare_managed_server_deployment`、`project_managed_deployment_evidence`、`refresh_deployment`、`check_deployment_routes`、`redeploy_deployment_path_version` 五个 IPC 的注册、签名和实现未改动。
- RED 为 `registered/source/bundled=90/45/45`、45 个 `registeredOnly`；GREEN 后三种审计模式均按预期 exit 1，精确计数为 `71/45/45`、26 个 `registeredOnly`，`missingRegistrations`、`bundleOnly`、`sourceNotBundled`、`dynamicInvocations` 均为 0。
- 五个恢复不变量具名测试均以完整模块名和 `--exact` 独立运行且各 1 项 PASS：`runtime_dependency_failures_resume_the_same_server_deploy`、`route_reconciliation_failures_resume_without_redeploying_the_application`、`public_route_failures_pause_without_rebuilding`、`repaired_deployment_path_routes_keep_existing_artifacts_on_the_server`、`interrupted_first_route_check_preserves_the_deployed_version`。`route` 过滤测试 18 项 PASS、`server_deploy` 过滤测试 1 项 PASS；未连接真实外部服务。

## Task 7 实施记录（2026-08-02）

- RED：`source` 与 `all` 审计均按设计 exit 1，精确为 `registered/source/bundled=71/45/45`；26 个 `registeredOnly` 与计划 Task 7 清单完全相等，其他差异与 `dynamicInvocations` 均为 0。
- GREEN：删除 26 个旧数据、配置、运行时秘密和 Provider 管理 adapter、command 属性与 `generate_handler!` 注册后，`source`、`bundle`、`all` 均 exit 0；精确为 `45/45/45`，`registeredOnly`、`missingRegistrations`、`bundleOnly`、`sourceNotBundled`、`dynamicInvocations` 均为 0。
- 11 个当前数据/Provider IPC 保持注册和签名：`list_connections`、`list_deployment_paths`、`save_deployment_path`、`list_deployment_path_runs`、`load_runtime_config`、`store_runtime_config`、`prepare_cnb_secret_bundle`、`replace_registry_credentials`、`check_saved_registry_credentials`、`ensure_cnb_repository`、`check_cnb_secret_repository_access`。
- schema 与 migration SQL 未改。旧 Profile、绑定、版本、验证和环境 projection 已没有生产 IPC caller，但仍是迁移、重启、秘密隔离和 current pointer 测试的查询/写入接缝；对应 7 个 model 与精确 Workspace accessor/helper 使用 `#[cfg(test)]`，没有增加 `dead_code` 许可。`WorkspaceState::settings`、`server_for_project` 等仍有生产 caller 的内部能力保持生产可见。
- `secret_status`、`store_secret`、`delete_secret` 的定义位于扩围批准的 `credentials.rs`；三者 callers 为 0，已连同 `SecretStatus` 删除。`replace_registry_credentials`、`check_saved_registry_credentials`、密钥替换回滚、错误码和脱敏链未改。未连接 CNB、服务器或其他真实外部服务。
- 保护证据：7 个完整模块名 `--lib --exact` 测试各真实运行 1 项并通过；`workspace` 过滤簇 32 项通过，`runtime_config` 过滤簇 4 项通过。仅删除 `existing_project_config_prefers_environment_specific_values`：它是只调用已退役 `load_existing_project_config` helper 的 endpoint-only 测试；没有删除或弱化迁移、Profile 秘密隔离、逐地址重启恢复、失败不移动在线版本/current run 的断言。

### Task 7 逐命令最终裁决

本表记录实际实施结果；若下方审计基线行仍描述实施前预计 caller，以本表与最终 CodeGraph/`rg` 证据为准。

| command | final decision | retained/deleted implementation | concrete evidence |
| --- | --- | --- | --- |
| `get_project_server` | `internalize` | 删除 IPC；保留 `WorkspaceState::server_for_project` 供当前部署与路由恢复读取服务器绑定。 | `capture_deployment_artifacts`、`verify_public_routes`、`deployment_server_profile` 生产 callers；旧 schema 幂等迁移测试。 |
| `get_project_connection_bindings` | `internalize` | 删除 IPC 与其专属 `reconcile_project_connection_bindings`；保留 `project_connection_bindings` 为 `#[cfg(test)]` 兼容查询接缝。 | 旧项目连接绑定幂等迁移、项目重连和秘密隔离测试。 |
| `delete_deployment_path` | `internalize` | 删除 IPC；保留 `delete_deployment_path` 为 `#[cfg(test)]` 事务接缝。 | `deployment_paths_are_project_scoped_reusable_connection_bindings` 验证活动任务期间拒绝删除线路。 |
| `list_config_profiles` | `internalize` | 删除 IPC；保留 Profile 读取与 model 为 `#[cfg(test)]` 兼容查询接缝。 | `stores_reusable_profiles_and_project_bindings_without_secret_values` 验证返回值不含秘密正文。 |
| `save_config_profile` | `internalize` | 删除 IPC 及 endpoint Keychain 写入；保留 Profile 写入为 `#[cfg(test)]` 迁移夹具接缝。 | Profile/绑定迁移测试以非秘密字段构造旧数据；当前生产无 Profile CRUD caller。 |
| `delete_config_profile` | `delete` | 删除 IPC、DTO、Profile secret-key helper 及 test-only `remove_config_profile`。 | CodeGraph/`rg` 无 endpoint、helper 或专属删除测试残留；Profile secret isolation、binding schema 升级与项目 relink 测试继续通过。 |
| `bind_config_profile` | `internalize` | 删除 IPC；保留绑定写入为 `#[cfg(test)]`。 | Profile 绑定增量 schema、项目移动重连测试。 |
| `list_config_profile_bindings` | `internalize` | 删除 IPC；保留绑定读取为 `#[cfg(test)]`。 | 旧 binding schema 升级后重启读取测试。 |
| `set_environment_config_bindings` | `internalize` | 删除 IPC；保留批量绑定写入为 `#[cfg(test)]`。 | `upgrades_profile_bindings_and_supports_multiple_profiles_per_environment`。 |
| `list_project_environments` | `internalize` | 删除 IPC；保留环境/current pointer projection 为 `#[cfg(test)]`。 | 失败或 needs-action 更新不替换在线版本；旧失败记录不伪造版本。 |
| `list_project_versions` | `internalize` | 删除 IPC；保留不可变版本 projection 为 `#[cfg(test)]`。 | 版本/制品回填、rollback 重启和摘要原子性测试。 |
| `list_version_validations` | `internalize` | 删除 IPC；保留验证读取为 `#[cfg(test)]`。 | 版本验证跨重启、同摘要共享与最新拒绝结果测试。 |
| `set_version_validation` | `internalize` | 删除 IPC；保留验证原子写入为 `#[cfg(test)]`。 | `persists_version_validation_across_workspace_restart` 及 immutable version 关联测试。 |
| `load_existing_project_config` | `delete` | 删除 runtime helper、DTO 与唯一 endpoint-only 单测。 | CodeGraph caller 仅该测试；当前 `load_runtime_config` 保持注册并通过 runtime_config 4 项回归。 |
| `runtime_config_sync_status` | `delete` | 删除 command/helper 与 sync DTO。 | CodeGraph callers=0；当前 server runtime、secret bundle 和错误码链不变。 |
| `sync_runtime_config_to_server` | `delete` | 删除 command/helper；其专属 `verified_server_profile` 同步删除。 | CodeGraph callers=0；当前 `prepare_cnb_secret_bundle` 仍使用身份指纹验证与脱敏配置。 |
| `runtime_secret_status` | `delete` | 删除旧逐变量 Keychain 状态 command 与 DTO。 | CodeGraph callers=0；whole-file `load_runtime_config` 保持。 |
| `store_runtime_secret` | `delete` | 删除旧逐变量 Keychain 写入 command。 | CodeGraph callers=0；`store_runtime_config` 和兼容 secret bundle 读取保持。 |
| `generate_runtime_secret` | `delete` | 删除旧逐变量随机 secret command。 | CodeGraph callers=0；当前内部 `load_or_generate_runtime_secret`/`generate_runtime_secret_value` 仍供部署链使用。 |
| `secret_status` | `delete` | 删除旧通用 Secret CRUD status command。 | CodeGraph callers=0；registry 凭据检查使用独立当前入口。 |
| `store_secret` | `delete` | 删除旧通用 Secret CRUD write command。 | CodeGraph callers=0；registry 凭据替换仍使用串行写入、回滚和 zeroize。 |
| `delete_secret` | `delete` | 删除旧通用 Secret CRUD delete command。 | CodeGraph callers=0；当前凭据轮换内部删除/回滚 helper 保持。 |
| `create_cnb_repository` | `delete` | 删除旧 token-in-argument 创建 command 与 DTO。 | CodeGraph callers=0；当前 `ensure_cnb_repository` 保持幂等仓库确保与稳定错误码。 |
| `check_cnb_repository_access` | `delete` | 删除旧 recent-builds 管理 command。 | CodeGraph callers=0；当前 secret repository access 检查保持。 |
| `enable_cnb_auto_trigger` | `delete` | 删除旧自动触发管理 command。 | CodeGraph callers=0；当前同步/部署 Provider 流程不调用该 endpoint。 |
| `get_app_settings` | `internalize` | 删除批量 IPC；保留 `WorkspaceState::settings`。 | `reconcile_compat_connections` 生产 caller；旧 Provider 设置回填且不暴露秘密测试。 |

## Task 8 孤儿实现终审（2026-08-02）

- 基线与 Green 均保持 `registered/source/bundled=45/45/45`；`missingRegistrations`、`registeredOnly`、`bundleOnly`、`sourceNotBundled`、`dynamicInvocations` 全为 0，`source`、`bundle`、`all` 三个 CLI 均 exit 0，命令面测试 12/12 PASS。
- 对全部 45 个 `delete` 行逐项运行 CodeGraph impact 与 `rg -n '<command>' apps/desktop`。RED 时 CodeGraph 对 42 个命令返回 `not found`；`secret_status`、`runtime_secret_status` 只命中同名 TypeScript DTO；`generate_runtime_secret` 只命中当前生产链 `load_or_generate_runtime_secret` / `generate_runtime_secret_value`。Green 后 CodeGraph 为 44/45 `not found`；`rg` 对 44 个命令为零命中，`generate_runtime_secret` 的 5 个命中均属于上述当前生产链，不是旧 endpoint。
- RED 清单是 `apps/desktop/src/types.ts` 中 22 个无任何前端生产或测试消费者的旧 DTO/类型；Green 删除 168 行：`CnbRepositoryInput`、`CnbRepositoryResult`、`SecretStatus`、`EnvironmentConnectionBindings`、`ProjectConnectionBindings`、`ConfigProfileKind`、`ConfigProfileScope`、`ConfigProfile`、`ConfigProfileInput`、`ProjectProfileBinding`、`EnvironmentConfigBindings`、`RuntimeConfigRecommendation`、`ExistingProjectConfig`、`LocalEnvWriteResult`、`LocalDevelopmentSupport`、`LocalInfrastructureStatus`、`RuntimeSecretStatus`、`RuntimeConfigSyncStatus`、`ProjectEnvironment`、`VersionValidationState`、`VersionValidation`、`ProjectVersion`。同名 Rust 兼容 model、schema、migration、current pointer 与保护测试均未改。
- 其余 `delete` 行没有 command、adapter、command attribute、注册、专属类型或端点测试残留。`ApplyResult`、`LocalPreviewStatus`、`RuntimeEnvironment`、`GeneratedSshIdentity` 等相邻类型仍有当前 IPC/生产 caller，因此明确保留；没有删除有效迁移、兼容、安全、恢复或进程退出断言。

### Task 8 internalize 复核

| commands | retained implementation | production caller or named compatibility evidence |
| --- | --- | --- |
| `begin_deployment_attempt` | `WorkspaceState::begin_deployment_attempt` | `redeploy_deployment_path_version`、`start_deployment_path_inner`、`refresh_deployment_path`、`take_over_deployment_path_routes_inner`、`run_pilot_validation` 生产 caller。 |
| `create_deployment_task` | `create_deployment_task_inner` | `prepare_managed_server_deployment` 与 `run_pilot_validation` 生产 caller。 |
| `get_app_settings` | `WorkspaceState::settings` | `reconcile_compat_connections` 生产 caller。 |
| `get_project_server` | `WorkspaceState::server_for_project` | `capture_deployment_artifacts`、`verify_public_routes`、`deployment_server_profile`、`remember_registered_domain_requirement` 生产 caller。 |
| `save_project_step` | `WorkspaceState::set_project_step` | `save_manifest_draft`、`refresh_deployment`、`run_pilot_validation` 生产 caller。 |
| `prepare_deployment_path_retry` | `prepare_deployment_path_retry_inner` | `run_pilot_validation` 生产 caller；`repaired_deployment_path_drops_the_previous_issue_code`、`repaired_deployment_path_routes_keep_existing_artifacts_on_the_server`。 |
| `start_deployment_path` | `start_deployment_path_inner` | `run_pilot_validation` 生产 caller。 |
| `take_over_deployment_path_routes`、`take_over_server_routes` | `take_over_deployment_path_routes_inner`、`take_over_caddy_routes` | `run_pilot_validation` 生产 caller；内部 route takeover 复用 `take_over_caddy_routes`。 |
| `delete_deployment_path` | `WorkspaceState::delete_deployment_path`（`#[cfg(test)]`） | `deployment_paths_are_project_scoped_reusable_connection_bindings`。 |
| `get_project_connection_bindings` | `WorkspaceState::project_connection_bindings`（`#[cfg(test)]`） | `removing_and_readding_a_project_starts_with_a_fresh_release_model`、`reset_clears_only_project_deployment_state_and_blocks_stale_resurrection`。 |
| `bind_config_profile`、`list_config_profile_bindings`、`list_config_profiles`、`save_config_profile`、`set_environment_config_bindings` | 精确 `#[cfg(test)]` Profile/绑定接缝 | `stores_reusable_profiles_and_project_bindings_without_secret_values`、`upgrades_profile_bindings_and_supports_multiple_profiles_per_environment`、`relinks_a_moved_project_without_losing_history_bindings_or_settings`。 |
| `list_deployment_attempts` | `WorkspaceState::list_deployment_attempts`，保留既有具名窄化 `dead_code` 原因 | `deployment_retries_append_attempts_and_freeze_the_path_snapshot`。 |
| `list_project_environments` | `WorkspaceState::list_project_environments`（`#[cfg(test)]`） | `newer_failed_or_needs_action_production_does_not_replace_online_version`、`restart_backfills_production_history_and_preserves_a_newer_rollback`。 |
| `list_project_versions` | `WorkspaceState::list_project_versions`（`#[cfg(test)]`） | `lists_immutable_project_versions_with_artifacts_validation_and_environment_ownership`、`failed_deployment_does_not_create_a_project_version`。 |
| `list_version_validations`、`set_version_validation` | 精确 `#[cfg(test)]` 版本验证读写接缝 | `persists_version_validation_across_workspace_restart`、`shares_validation_between_runs_with_the_same_immutable_artifacts`、`latest_rejected_result_replaces_the_previous_passed_result`。 |

## Task 11 最终文档事实（2026-08-02）

- 工作树根下 `node scripts/check-desktop-command-surface.mjs --mode all --json` 退出 0：`registered/source/bundled=45/45/45`，`dynamicInvocations`、`missingRegistrations`、`registeredOnly`、`bundleOnly` 和 `sourceNotBundled` 均为 0。三个数字分别来自 `lib.rs` 的 `tauri::generate_handler!`、`api.ts` / `api/` 生产源码与 Vite bundle；本节不再复制 45 个命令名。
- 门禁入口为根 `check:desktop-command-surface`、`check:project` 同进程 source 审计，以及桌面 `build` 的 post-Vite bundle 审计；三者共用 `scripts/lib/desktop-command-surface.mjs`，没有平行的命令名单。
- 相对实施计划 base `24e0fc79f9bda0401362b4e93cb570774b4abfa8`，Task 11 最终工作树为 45 个跟踪文件、2,611 行新增、5,602 行删除，净减少 2,991 行；其中包含本 change 的证据、门禁和测试资产。
- `pnpm codegraph:index`、`pnpm codegraph:sync` 和 `pnpm codegraph:status` 均在实际 worktree root 运行；最终索引为 216 个文件、3,006 个节点和 8,936 条边，且无 pending/stale 文件。`.codegraph/` 由 `.gitignore` 忽略，未作为变更资产提交。
- 以上只是静态、构建和自动回归证据。Task 11 没有运行桌面客户端启动或当前主线的无副作用烟测，不新增用户验收。

## Task 12 最终验证事实（2026-08-02）

- 18:03–18:32 CST 在绑定 worktree 根目录完成 fresh 验证。`pnpm check:project` exit 0（354 个文件、17 个必需文档），`pnpm check:secrets` exit 0（352 个跟踪文件），OpenSpec strict exit 0。命令面 Node 测试初次暴露本矩阵 `save_config_profile` 理由未重复具名证据；只修正允许范围内的矩阵文字后 fresh 重跑为 23/23、exit 0，未修改生产代码。
- 完整 `pnpm check` exit 0：Rust desktop 134 项、deploy-core 108 项、integration 3 项，Vitest 36 files / 195 tests，release-manifest 2 项，desktop/site build、TypeScript、格式和 Clippy 均通过。desktop build 内再次得到 `registered/source/bundled=45/45/45`；fresh `--mode all --json` 同样 exit 0，`dynamicInvocations` 与四类差异均为 0。
- 最终命令面相对初始 `registered=111` 删除 66 个公开注册 endpoint，保留 45 个生产 IPC；删除 4 个无生产用途的 runtime 依赖（3 个前端包和 `tauri-plugin-clipboard-manager`）。相对计划 base `24e0fc79f9bda0401362b4e93cb570774b4abfa8` 的最终跟踪差异为 45 个文件、2,637 行新增、5,604 行删除，净减少 2,967 行；该统计包含变更证据、门禁和测试资产。
- worktree-local `pnpm codegraph:index`、`pnpm codegraph:sync`、`pnpm codegraph:status` 均 exit 0：216 个文件、3,006 个节点、8,936 条边，无 pending/stale 文件；忽略的 `.codegraph/` 没有进入跟踪状态。
- `pnpm tauri:build:app` 于 18:05:38 CST 开始并 exit 0，构建产物 mtime 为 18:08:40 CST。精确路径为 `/Users/chanjack/Documents/DeployWorkspace/deploydesk/.worktrees/retire-legacy-desktop-command-surface/target/release/bundle/macos/ABCDeploy.app`；可执行文件为 thin arm64。`codesign --verify --deep --strict --verbose=2` exit 0，Identifier 为 `cloud.finagent.abcdeploy`，Authority 为 `Apple Development: blacksco@163.com (PGL5DHKHQH)`；本地测试包按设计未 notarize，未生成 DMG 或任何发布资产。

| Computer Use 无副作用烟测 | 结果 | fresh AX 证据与边界 |
| --- | --- | --- |
| 精确构建 app 启动并到达“我的部署” | `PASS` | 使用 node_repl + Computer Use plugin wrapper 以精确 `.app` 路径启动；AX 读到“我的部署”、`新建部署` 和 5 条已保存记录，没有 `command not found`。解锁后 `list_apps` 显示两个 bundle id 同为 `cloud.finagent.abcdeploy` 的实例；后续读取与操作始终使用 worktree `.app` 精确路径。 |
| 已有部署列表、详情与更新入口恢复 | `PASS` | `wx` 的在线服务器详情恢复当前状态、地址、源码和运行版本；点击 `更新上线` 后进入服务器更新编辑器，预检显示“可以开始 / 服务器连接已经验证”，且没有 `command not found`。未点击最终“更新上线”，没有服务器变更。 |
| 系统文件夹选择器打开并取消 | `PASS` | 首轮点击后曾因 Mac 锁定无法确认；解锁后精确 app 初始 AX 明确为系统 `Open` sheet，证明此前文件夹选择器已经打开。按 Escape 回到新建页，未选择目录。 |
| 外部准备链接打开 | `NOT_TESTED` | “连接新服务器” modal 和准备说明均正常展开；点击“查看购买服务器说明”后应用无报错且打开动作已派发，但以 Chrome 精确 bundle id 读取返回 `noWindowsAvailable`，目标窗口不可观察。未登录、提交或传输数据，因此不写 PASS 或 FAIL。 |
| 复制应用内非敏感准备文本 | `NOT_TESTED` | 当前 copy action 仅在 `CnbSecretHandoffDialog` 的 Provider 授权流程可达；为避免连接 Provider 或产生外部副作用未进入该流程，也未复制任何凭据、路径或敏感文本。 |
| 本机当前主线入口实际打开 | `PASS` | 打开既有待处理记录 `swifteng` 后恢复并预填本地项目；选择“这台电脑”完成预检，显示“可以开始 / 这台电脑可以接收本次运行”，没有 `command not found`。未点击“在本机运行”，没有启动服务。 |
| 服务器当前主线入口实际打开 | `PASS` | 在同一 `swifteng` 页面选择“服务器”后显示已保存的已验证服务器与“连接新服务器”；选择该服务器完成预检，显示“可以开始 / 服务器连接已经验证”，没有 `command not found`。未点击“上线到服务器”，没有新建连接、上线或修改服务器；`wx` 的更新入口也独立到达同一 ready 门禁。 |

- 首轮 folder 入口后以显示名读取时意外命中另一同名 legacy ABCDeploy 窗口，并看到其既有数据库约束 toast；该窗口不是精确构建 app 的证据，明确排除在本次回归结论之外。随后只读 `list_apps` 报告 Mac 已锁定，按 Computer Use 安全规则立即停止全部 UI 操作且不尝试解锁。解锁后的补测先确认存在两个同 bundle id 实例，再全程用 worktree `.app` 精确路径定向；首轮异常经过保留，但不覆盖这些 fresh 精确路径证据。
- 本节仍只记录自动证据和有限的无副作用 smoke，不产生用户验收；本机运行、更新和恢复继续是 `IMPLEMENTED_UNVERIFIED`，原服务器正向主线的既有 `VERIFIED` 事实不变。文件夹选择器取消、本机 ready 入口和服务器 ready 入口记为 `PASS`；外链目标窗口不可观察与 Provider 授权内复制保持 `NOT_TESTED`，均无功能 `FAIL`。

## 矩阵

| command                                  | feature consumer                                                                                                                                                                                                                                                                  | TypeScript wrapper                                                          | production bundle | Rust internal callers                                                                                                                                                              | data/migration duty                                    | tests                                                                                                                                             | decision      | rationale                                                                                                                       |
| ---------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------- | ----------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- | ------------------------------------------------------------------------------------------------------------------------------- |
| `apply_manifest` | 无 | — | no | Task 4 后 `rg` 无 handler、command 属性或注册残留；共享 `apply_plan` 的生产 caller 为 `pilot_validation::run_pilot_validation` 和 `crates/deployctl/src/main.rs` | 无必须保留的数据/迁移职责 | `cargo test -p abcdeploy-desktop manifest -- --nocapture`（4 PASS）；`plan_is_previewable_and_apply_creates_backups`；adoption 保护测试 | `delete` | 已删除旧完整 apply IPC；保留 `save_manifest_draft`，以及上述两个精确 caller 复用的 Manifest/plan 内核。 |
| `begin_deployment_attempt`               | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | Task 6 后仅保留 `WorkspaceState::begin_deployment_attempt`；被 `redeploy_deployment_path_version`、`refresh_deployment_path`、`run_pilot_validation`、`take_over_deployment_path_routes_inner` 调用 | 追加 attempt，冻结线路快照并支撑失败重试               | `deployment_retries_append_attempts_and_freeze_the_path_snapshot`; `current_run_query_keeps_the_online_version_after_a_failed_update`             | `internalize` | 已删除 Tauri adapter 与注册；保留 `WorkspaceState::begin_deployment_attempt` 的 attempt 追加与线路快照冻结职责。                 |
| `bind_config_profile`                    | 无；仅 API 再导出/包装                                                                                                                                                                                                                                                            | `bindConfigProfile` in `apps/desktop/src/api/config-profiles.ts`            | no                | `WorkspaceState::bind_config_profile`（迁移/重连测试直接调用）                                                                                                                     | 旧 profile binding 的增量 schema 与项目重连            | `stores_reusable_profiles_and_project_bindings_without_secret_values`; `upgrades_profile_bindings_and_supports_multiple_profiles_per_environment` | `internalize` | 删除 handler；保留 `WorkspaceState::bind_config_profile` 的旧 profile binding 迁移与重连职责。                                  |
| `bind_project_server`                    | `apps/desktop/src/features/deployment-editor/server-connection.ts`                                                                                                                                                                                                                | `bindProjectServer` in `apps/desktop/src/api.ts`                            | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | Deployment aggregate、线路、current pointer 或恢复事实 | server-deployment/DeploymentEditor Feature tests; `src-tauri/src/tests.rs`; Workspace tests                                                       | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `bootstrap_server_caddy`                 | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | Task 6 后 `rg` 无 endpoint、实现或注册残留                                                                                                                                           | 无必须保留的数据/迁移职责                              | route/server_deploy 共享行为测试                                                                                          | `delete`      | 已删除旧 Caddy bootstrap endpoint 及专属实现；当前部署路由内核不变。                |
| `cancel_local_preview_start` | 无 | — | no | Task 5 后 `rg` 无 handler、command 属性或注册残留；取消状态机保留在 `local_process.rs`，测试通过 `cancel_local_start` 驱动，并有界轮询对应 `LOCAL_START_PROCESSES` key 到真实 `Some(pid)` 后才取消 | 无必须保留的数据/迁移职责；共享进程表与退出清理继续服务当前本机启动 | `retained_local_run_cancellation_stops_a_running_child_process` 取消前证明 Unix 进程组存活、取消后证明进程组退出并断言 `Interrupted`；`stops_a_silent_local_command_instead_of_waiting_forever` | `delete` | 已删除旧取消 IPC；保留 `LocalStartTask`、受管进程表、进程组终止和应用退出清理；测试不再依赖固定 sleep 提前取消而假绿。 |
| `check_cnb_repository_access`            | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | Workspace/runtime_config 保护测试；无独立 endpoint 存在性要求                                                                                     | `delete`      | 旧数据/配置/秘密/Provider 管理 endpoint 无生产消费者；未列入 internalize 的实现无当前主线或迁移职责。                           |
| `check_cnb_secret_repository_access`     | `apps/desktop/src/features/deployment-editor/server-deployment-authorization.ts`                                                                                                                                                                                                  | `checkCnbSecretRepositoryAccess` in `apps/desktop/src/api.ts`               | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | 连接/秘密引用；秘密正文保持 Keychain 边界              | server-deployment/DeploymentEditor Feature tests; `src-tauri/src/tests.rs`; Workspace tests                                                       | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `check_deployment_routes`                | `apps/desktop/src/features/deployment-editor/deployment-editor-services.ts`                                                                                                                                                                                                       | `checkDeploymentRoutes` in `apps/desktop/src/api.ts`                        | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | Deployment aggregate、线路、current pointer 或恢复事实 | server-deployment/DeploymentEditor Feature tests; `src-tauri/src/tests.rs`; Workspace tests                                                       | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `check_docker` | 无 | — | no | Task 4 后 `rg` 无 handler、command 属性或注册残留 | 无必须保留的数据/迁移职责 | `cargo clippy --workspace --all-targets --locked -- -D warnings`；104/45/45 命令面审计 | `delete` | 已删除旧通用 Docker 检查 IPC 及其唯一 `docker` 模块导入；当前运行环境检查入口不变。 |
| `check_registry_credentials`             | 无 Tauri Feature consumer；仅 `replaceRegistryCredentials` 浏览器回退                                                                                                                                                                                                             | `checkRegistryCredentials` in `apps/desktop/src/api/secrets.ts`             | yes               | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | `apps/desktop/src/api.test.ts` 的旧包装/浏览器契约；无 Tauri Feature 行为保护                                                                     | `delete`      | bundle 字符串仅由 `replaceRegistryCredentials` 的浏览器回退保活；Tauri 分支无 Feature caller，改为纯浏览器校验后删除 endpoint。 |
| `check_saved_registry_credentials`       | `apps/desktop/src/features/deployment-editor/server-deployment-setup-service.ts`                                                                                                                                                                                                  | `checkSavedRegistryCredentials` in `apps/desktop/src/api/secrets.ts`        | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | 连接/秘密引用；秘密正文保持 Keychain 边界              | server-deployment/DeploymentEditor Feature tests; `src-tauri/src/tests.rs`; Workspace tests                                                       | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `check_server`                           | `apps/desktop/src/features/deployment-editor/server-connection.ts`                                                                                                                                                                                                                | `checkServer` in `apps/desktop/src/api.ts`                                  | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | 连接/秘密引用；秘密正文保持 Keychain 边界              | server-deployment/DeploymentEditor Feature tests; `src-tauri/src/tests.rs`; Workspace tests                                                       | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `connect_cnb`                            | `apps/desktop/src/features/deployment-editor/server-deployment-authorization.ts`                                                                                                                                                                                                  | `connectCnb` in `apps/desktop/src/api.ts`                                   | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | 连接/秘密引用；秘密正文保持 Keychain 边界              | server-deployment/DeploymentEditor Feature tests; `src-tauri/src/tests.rs`; Workspace tests                                                       | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `continue_existing_deployment`           | `apps/desktop/src/features/deployment-editor/deployment-editor-services.ts`                                                                                                                                                                                                       | `continueExistingDeployment` in `apps/desktop/src/api.ts`                   | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | Deployment aggregate、线路、current pointer 或恢复事实 | server-deployment/DeploymentEditor Feature tests; `src-tauri/src/tests.rs`; Workspace tests                                                       | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `create_cnb_repository`                  | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | Workspace/runtime_config 保护测试；无独立 endpoint 存在性要求                                                                                     | `delete`      | 旧数据/配置/秘密/Provider 管理 endpoint 无生产消费者；未列入 internalize 的实现无当前主线或迁移职责。                           |
| `create_deployment_task`                 | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | Task 6 后仅保留 `create_deployment_task_inner`；由 `mvp_environment::prepare_managed_server_deployment`、`pilot_validation::run_pilot_validation` 调用                                                 | 创建固定输入的 deployment run 与线路绑定               | `runtime_dependency_failures_resume_the_same_server_deploy`; server deploy tests                                                                  | `internalize` | 已删除 Tauri adapter 与注册；保留 `create_deployment_task_inner` 为当前入口创建固定输入并绑定线路。              |
| `create_managed_local_run_workspace`     | `apps/desktop/src/features/deployment-editor/deployment-editor-services.ts`                                                                                                                                                                                                       | `createManagedLocalRunWorkspace` in `apps/desktop/src/api.ts`               | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | Deployment aggregate、线路、current pointer 或恢复事实 | `DeploymentEditorController.test.tsx`; `source_snapshots.rs` tests; `deployment-evidence/model.test.ts`                                           | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `delete_config_profile`                  | 无；仅 API 再导出/包装                                                                                                                                                                                                                                                            | `deleteConfigProfile` in `apps/desktop/src/api/config-profiles.ts`          | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | `apps/desktop/src/api.test.ts` 的旧包装/浏览器契约；无 Tauri Feature 行为保护                                                                     | `delete`      | 包装无 Feature caller并被生产 tree-shaking 移除；无独立 Rust caller 或必须保留的数据职责。                                      |
| `delete_deployment_path`                 | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | `WorkspaceState::delete_deployment_path`（事务保护测试直接调用）                                                                                                                   | 活动任务期间拒绝删除线路的事务约束                     | `deployment_paths_are_project_scoped_reusable_connection_bindings`                                                                                | `internalize` | 删除 handler；保留 `WorkspaceState::delete_deployment_path` 的活动任务删除事务约束。                                            |
| `delete_secret`                          | 无；仅 API 再导出/包装                                                                                                                                                                                                                                                            | `deleteSecret` in `apps/desktop/src/api/secrets.ts`                         | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | `apps/desktop/src/api.test.ts` 的旧包装/浏览器契约；无 Tauri Feature 行为保护                                                                     | `delete`      | 包装无 Feature caller并被生产 tree-shaking 移除；无独立 Rust caller 或必须保留的数据职责。                                      |
| `detect_dns_provider`                    | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | Task 6 后 `rg` 无 endpoint、专属 provider 映射或 `DnsProviderHint` 类型残留                                                                                                                                           | 无必须保留的数据/迁移职责                              | route/server_deploy 共享行为测试                                                                                          | `delete`      | 已删除 endpoint-only DNS provider 探测、Rust/TypeScript 类型和专属测试；共享 DNS 查询仍保留。                |
| `discover_ssh_identities` | 无 | — | no | Task 4 后 handler、command 属性、注册及专属 `discover_identities` / `discover_identities_in` / `expand_home` 枚举链均已删除 | 无必须保留的数据/迁移职责；`SshIdentity` 与 `identity_from_path` 继续服务受管身份生成 | `creates_and_reuses_a_managed_ed25519_identity` 保留创建、复用、公钥、文件、managed 与 fingerprint 断言；104/45/45 命令面审计 | `delete` | 已删除旧本机 SSH 身份枚举 IPC 及无生产 caller 的专属枚举链；保留 `generate_ssh_identity`、`generate_managed_identity` 与受管身份生成行为。 |
| `enable_cnb_auto_trigger`                | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | Workspace/runtime_config 保护测试；无独立 endpoint 存在性要求                                                                                     | `delete`      | 旧数据/配置/秘密/Provider 管理 endpoint 无生产消费者；未列入 internalize 的实现无当前主线或迁移职责。                           |
| `ensure_cnb_repository`                  | `apps/desktop/src/features/deployment-editor/server-deployment-authorization.ts`                                                                                                                                                                                                  | `ensureCnbRepository` in `apps/desktop/src/api.ts`                          | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | 连接/秘密引用；秘密正文保持 Keychain 边界              | server-deployment/DeploymentEditor Feature tests; `src-tauri/src/tests.rs`; Workspace tests                                                       | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `forget_project`                         | `apps/desktop/src/App.tsx`                                                                                                                                                                                                                                                        | `forgetProject` in `apps/desktop/src/api.ts`                                | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | Deployment aggregate、线路、current pointer 或恢复事实 | `apps/desktop/src/api.test.ts`; adjacent Feature tests; `src-tauri/src/tests.rs`                                                                  | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `generate_runtime_secret`                | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | Workspace/runtime_config 保护测试；无独立 endpoint 存在性要求                                                                                     | `delete`      | 旧数据/配置/秘密/Provider 管理 endpoint 无生产消费者；未列入 internalize 的实现无当前主线或迁移职责。                           |
| `generate_ssh_identity`                  | `apps/desktop/src/features/deployment-editor/server-connection.ts`                                                                                                                                                                                                                | `generateSshIdentity` in `apps/desktop/src/api.ts`                          | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | —                                                      | `apps/desktop/src/api.test.ts`; adjacent Feature tests; `src-tauri/src/tests.rs`                                                                  | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `get_app_setting`                        | `apps/desktop/src/features/deployment-editor/server-deployment-setup-service.ts`                                                                                                                                                                                                  | `getAppSetting` in `apps/desktop/src/api/settings.ts`                       | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | 应用非秘密设置                                         | `apps/desktop/src/api.test.ts`; adjacent Feature tests; `src-tauri/src/tests.rs`                                                                  | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `get_app_settings`                       | 无；仅 API 再导出/包装                                                                                                                                                                                                                                                            | `getAppSettings` in `apps/desktop/src/api/settings.ts`                      | no                | `reconcile_compat_connections` 调用 `WorkspaceState::settings`                                                                                                                     | 旧 Provider 设置到 Connection 的兼容回填               | `connection_resources_backfill_legacy_providers_without_exposing_secrets`                                                                         | `internalize` | 删除 handler；保留 `WorkspaceState::settings`，供 `reconcile_compat_connections` 回填旧 Provider 设置。                         |
| `get_cnb_account`                        | `apps/desktop/src/features/deployment-editor/server-deployment-authorization.ts`                                                                                                                                                                                                  | `getCnbAccount` in `apps/desktop/src/api.ts`                                | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | 连接/秘密引用；秘密正文保持 Keychain 边界              | server-deployment/DeploymentEditor Feature tests; `src-tauri/src/tests.rs`; Workspace tests                                                       | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `get_local_development_support` | 无 | — | no | Task 5 后 `rg` 无 handler、command 属性、注册或 `LocalDevelopmentSupport` 残留 | 无必须保留的数据/迁移职责；开发 Compose 内核仍由 `start_local_preview` 的 development mode 使用 | `generates_reliable_local_development_commands_without_changing_release_files`；本机 `local_` 过滤测试 | `delete` | 已删除旧开发支持探测 IPC 及专属状态类型；保留当前启动所需开发运行辅助能力。 |
| `get_local_infrastructure_status` | 无 | — | no | Task 5 后 `rg` 无 handler、command 属性、注册或专属状态读取辅助残留 | 无必须保留的数据/迁移职责；Keychain 基础设施 secret 内核仍由当前 runtime config 链使用 | Rust workspace/Clippy 门禁；90/45/45 命令面审计 | `delete` | 已删除旧本机基础设施状态 IPC、专属状态类型和 Docker 列表解析链。 |
| `get_local_preview_status` | 无 | — | no | Task 5 后 `rg` 无 handler、command 属性、注册或 `planned_local_preview_status` 残留；`local_preview_status` 继续被当前启动与验证使用 | 无必须保留的数据/迁移职责 | `source_snapshots::tests`；本机 `local_` 过滤测试 | `delete` | 已删除旧轮询状态 IPC 及专属 plan 投影；保留当前启动/验证共享状态投影。 |
| `get_preflight` | 无 | — | no | Task 4 后 `rg` 无 handler、command 属性或注册残留 | 无必须保留的数据/迁移职责 | `cargo test -p abcdeploy-desktop open_project -- --nocapture`（PASS，0 tests）；adoption 保护测试 | `delete` | 已删除旧系统预检 IPC 及仅由它使用的 preflight 导入；当前项目打开/恢复入口不变。 |
| `get_project_connection_bindings`        | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | `WorkspaceState::project_connection_bindings`（兼容回填测试直接调用）                                                                                                              | 旧项目连接绑定回填且不读取秘密正文                     | `migrates_legacy_workspace_model_idempotently`; `relinks_a_moved_project_without_losing_history_bindings_or_settings`                             | `internalize` | 删除 handler；保留 `WorkspaceState::project_connection_bindings` 的旧项目连接绑定幂等回填职责。                                 |
| `get_project_server`                     | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | `capture_deployment_artifacts`、`verify_public_routes`、`deployment_server_profile` 调用 `WorkspaceState::server_for_project`                                                      | 旧 server binding 与当前部署/路由恢复兼容              | `migrates_legacy_workspace_model_idempotently`; `upgrades_existing_server_records_with_host_fingerprints`                                         | `internalize` | 删除 handler；保留 `WorkspaceState::server_for_project`，供 `capture_deployment_artifacts` 恢复项目服务器绑定。                 |
| `inspect_server_route_conflicts`         | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | Task 6 后 `rg` 无 endpoint、专属冲突类型或检查实现残留                                                                                                                                           | 无必须保留的数据/迁移职责                              | route/server_deploy 共享行为测试                                                                                          | `delete`      | 已删除旧冲突检查 endpoint 与专属实现；当前 `check_deployment_routes` 保持不变。                |
| `install_server_key_with_password`       | `apps/desktop/src/features/deployment-editor/server-connection.ts`                                                                                                                                                                                                                | `installServerKeyWithPassword` in `apps/desktop/src/api.ts`                 | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | 连接/秘密引用；秘密正文保持 Keychain 边界              | server-deployment/DeploymentEditor Feature tests; `src-tauri/src/tests.rs`; Workspace tests                                                       | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `list_active_deployment_runs`            | `apps/desktop/src/features/deployment-list/use-deployment-dashboard.ts`                                                                                                                                                                                                           | `listActiveDeploymentRuns` in `apps/desktop/src/api.ts`                     | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | Deployment aggregate、线路、current pointer 或恢复事实 | `use-deployment-dashboard.test.ts`; `ProjectGallery.test.tsx`; Workspace tests                                                                    | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `list_attention_deployment_runs`         | `apps/desktop/src/features/deployment-list/use-deployment-dashboard.ts`                                                                                                                                                                                                           | `listAttentionDeploymentRuns` in `apps/desktop/src/api.ts`                  | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | Deployment aggregate、线路、current pointer 或恢复事实 | `use-deployment-dashboard.test.ts`; `ProjectGallery.test.tsx`; Workspace tests                                                                    | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `list_config_profile_bindings`           | 无；仅 API 再导出/包装                                                                                                                                                                                                                                                            | `listConfigProfileBindings` in `apps/desktop/src/api/config-profiles.ts`    | no                | `WorkspaceState::config_profile_bindings`（迁移/重连测试直接调用）                                                                                                                 | 旧 profile binding 迁移及重启读取                      | `upgrades_profile_bindings_and_supports_multiple_profiles_per_environment`                                                                        | `internalize` | 删除 handler；保留 `WorkspaceState::config_profile_bindings` 的 binding 迁移后重启读取职责。                                    |
| `list_config_profiles`                   | 无；仅 API 再导出/包装                                                                                                                                                                                                                                                            | `listConfigProfiles` in `apps/desktop/src/api/config-profiles.ts`           | no                | `WorkspaceState::list_config_profiles`（配置迁移测试读取）                                                                                                                         | Config Profile 旧数据读取；本机基础设施仍保存 profile  | `stores_reusable_profiles_and_project_bindings_without_secret_values`                                                                             | `internalize` | 删除 handler；保留 `WorkspaceState::list_config_profiles` 的 profile 读取且不暴露秘密正文职责。                                 |
| `list_connections`                       | `apps/desktop/src/features/deployment-editor/server-deployment-setup-service.ts`                                                                                                                                                                                                  | `listConnections` in `apps/desktop/src/api.ts`                              | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | 连接/秘密引用；秘密正文保持 Keychain 边界              | server-deployment/DeploymentEditor Feature tests; `src-tauri/src/tests.rs`; Workspace tests                                                       | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `list_current_deployment_runs`           | `apps/desktop/src/features/deployment-list/use-deployment-dashboard.ts`                                                                                                                                                                                                           | `listCurrentDeploymentRuns` in `apps/desktop/src/api.ts`                    | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | Deployment aggregate、线路、current pointer 或恢复事实 | `use-deployment-dashboard.test.ts`; `ProjectGallery.test.tsx`; Workspace tests                                                                    | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `list_deployment_attempts`               | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | Task 6 后仅保留 `WorkspaceState::list_deployment_attempts`，供 attempt 事务测试与内部审计读取；带原因的窄化 `dead_code` 许可                                                                                                                 | attempt 追加历史与重试审计                             | `deployment_retries_append_attempts_and_freeze_the_path_snapshot`                                                                                 | `internalize` | 已删除 Tauri adapter 与注册；保留 `WorkspaceState::list_deployment_attempts` 的历史读取能力，不扩大 IPC。                                     |
| `list_deployment_path_runs`              | `apps/desktop/src/features/deployment-detail/deployment-detail-services.ts`                                                                                                                                                                                                       | `listDeploymentPathRuns` in `apps/desktop/src/api.ts`                       | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | Deployment aggregate、线路、current pointer 或恢复事实 | `deployment-detail/model.test.ts`; `ProjectGallery.test.tsx`; Workspace/Tauri tests                                                               | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `list_deployment_paths`                  | `apps/desktop/src/App.tsx`<br>`apps/desktop/src/features/deployment-detail/deployment-detail-services.ts`<br>`apps/desktop/src/features/deployment-editor/deployment-editor-services.ts`<br>`apps/desktop/src/features/deployment-editor/server-deployment-setup-service.ts`      | `listDeploymentPaths` in `apps/desktop/src/api.ts`                          | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | Deployment aggregate、线路、current pointer 或恢复事实 | `deployment-detail/model.test.ts`; `ProjectGallery.test.tsx`; Workspace/Tauri tests                                                               | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `list_deployment_runs`                   | `apps/desktop/src/features/deployment-editor/deployment-editor-services.ts`                                                                                                                                                                                                       | `listDeploymentRuns` in `apps/desktop/src/api.ts`                           | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | Deployment aggregate、线路、current pointer 或恢复事实 | server-deployment/DeploymentEditor Feature tests; `src-tauri/src/tests.rs`; Workspace tests                                                       | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `list_project_environments`              | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | `WorkspaceState::list_project_environments`（当前指针/迁移测试读取）                                                                                                               | 旧环境表、current version/run 指针与失败不覆盖在线版本 | `migrates_legacy_failed_only_project_without_inventing_a_version`; `newer_failed_or_needs_action_production_does_not_replace_online_version`      | `internalize` | 删除 handler；保留 `WorkspaceState::list_project_environments` 的在线版本指针与失败隔离职责。                                   |
| `list_project_versions`                  | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | `WorkspaceState::list_project_versions`（版本回填/恢复测试读取）                                                                                                                   | 版本/制品回填、恢复历史与不可变摘要                    | `restart_backfills_production_history_and_preserves_a_newer_rollback`; `production_digest_mismatch_is_atomic_and_does_not_switch_pointer`         | `internalize` | 删除 handler；保留 `WorkspaceState::list_project_versions` 的版本回填、恢复历史与摘要读取职责。                                 |
| `list_recent_projects`                   | `apps/desktop/src/features/deployment-list/use-deployment-dashboard.ts`                                                                                                                                                                                                           | `listRecentProjects` in `apps/desktop/src/api.ts`                           | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | Deployment aggregate、线路、current pointer 或恢复事实 | `use-deployment-dashboard.test.ts`; `ProjectGallery.test.tsx`; Workspace tests                                                                    | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `list_recent_successful_deployment_runs` | `apps/desktop/src/features/deployment-list/use-deployment-dashboard.ts`                                                                                                                                                                                                           | `listRecentSuccessfulDeploymentRuns` in `apps/desktop/src/api.ts`           | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | Deployment aggregate、线路、current pointer 或恢复事实 | `use-deployment-dashboard.test.ts`; `ProjectGallery.test.tsx`; Workspace tests                                                                    | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `list_servers`                           | `apps/desktop/src/features/deployment-editor/deployment-editor-services.ts`<br>`apps/desktop/src/features/deployment-editor/server-deployment-authorization.ts`<br>`apps/desktop/src/features/deployment-editor/server-deployment-setup-service.ts`                               | `listServers` in `apps/desktop/src/api.ts`                                  | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | 连接/秘密引用；秘密正文保持 Keychain 边界              | server-deployment/DeploymentEditor Feature tests; `src-tauri/src/tests.rs`; Workspace tests                                                       | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `list_version_validations`               | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | `WorkspaceState::list_version_validations`（重启验证测试读取）                                                                                                                     | 版本验证跨重启持久化                                   | `persists_version_validation_across_workspace_restart`                                                                                            | `internalize` | 删除 handler；保留 `WorkspaceState::list_version_validations` 的跨重启验证读取职责。                                            |
| `load_existing_project_config`           | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | Workspace/runtime_config 保护测试；无独立 endpoint 存在性要求                                                                                     | `delete`      | 旧数据/配置/秘密/Provider 管理 endpoint 无生产消费者；未列入 internalize 的实现无当前主线或迁移职责。                           |
| `load_runtime_config`                    | `apps/desktop/src/features/deployment-editor/deployment-editor-services.ts`<br>`apps/desktop/src/features/deployment-editor/use-runtime-configuration-recovery.ts`                                                                                                                | `loadRuntimeConfig` in `apps/desktop/src/api.ts`                            | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | Deployment aggregate、线路、current pointer 或恢复事实 | server-deployment/DeploymentEditor Feature tests; `src-tauri/src/tests.rs`; Workspace tests                                                       | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `open_project`                           | `apps/desktop/src/App.tsx`<br>`apps/desktop/src/features/deployment-editor/deployment-editor-services.ts`<br>`apps/desktop/src/features/deployment-editor/server-deployment-authorization.ts`<br>`apps/desktop/src/features/deployment-editor/server-deployment-setup-service.ts` | `openProject` in `apps/desktop/src/api.ts`                                  | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | Deployment aggregate、线路、current pointer 或恢复事实 | `apps/desktop/src/api.test.ts`; adjacent Feature tests; `src-tauri/src/tests.rs`                                                                  | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `open_staging_preview_tunnel`            | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | Task 6 后 `rg` 无 endpoint、tunnel 进程表、类型、启动或退出清理残留                                                                                                                                           | 无必须保留的数据/迁移职责                              | route/server_deploy 共享行为测试                                                                                          | `delete`      | 已删除旧预览 tunnel 全链；不影响部署 route 检查。                |
| `pause_deployment_task`                  | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | Task 6 后 `rg` 无 endpoint 或专属实现残留                                                                                                                                           | 无必须保留的数据/迁移职责                              | route/server_deploy 共享行为测试                                                                                          | `delete`      | 已删除旧手工暂停入口；当前状态转换与可恢复失败逻辑仍由受保护流程使用。                |
| `prepare_cnb_secret_bundle`              | `apps/desktop/src/features/deployment-editor/server-deployment-authorization.ts`                                                                                                                                                                                                  | `prepareCnbSecretBundle` in `apps/desktop/src/api.ts`                       | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | 连接/秘密引用；秘密正文保持 Keychain 边界              | server-deployment/DeploymentEditor Feature tests; `src-tauri/src/tests.rs`; Workspace tests                                                       | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `prepare_deployment_path_retry`          | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | Task 6 后仅保留 `prepare_deployment_path_retry_inner`；由 `pilot_validation::run_pilot_validation` 调用                                                                                                | 按 server/routes 恢复点续跑且保留制品                  | `repaired_deployment_path_routes_keep_existing_artifacts_on_the_server`                                                                           | `internalize` | 已删除 Tauri adapter 与注册；保留 `prepare_deployment_path_retry_inner` 的 server/routes 恢复点续跑与制品保留职责。                            |
| `prepare_local_development` | 无 | — | no | Task 5 后 `rg` 无 handler、command 属性或注册残留；`write_local_development_compose` 与 build-service 选择仍由当前启动调用 | 无必须保留的数据/迁移职责 | `generates_reliable_local_development_commands_without_changing_release_files`；本机 `local_` 过滤测试 | `delete` | 已删除旧独立准备 IPC；保留 `start_local_preview` development mode 的 Compose/运行时内核。 |
| `prepare_local_infrastructure` | 无 | — | no | Task 5 后 `rg` 无 handler、command 属性、注册或专属 Compose/端口/profile 写入链残留 | 无必须保留的数据/迁移职责；`local_infrastructure_secret` 继续服务当前 runtime config | Rust workspace/Clippy 门禁；90/45/45 命令面审计 | `delete` | 已删除旧本机 PostgreSQL/Redis 自动准备 IPC 及专属死实现，不影响当前受管本机运行入口。 |
| `prepare_local_preview` | 无 | — | no | Task 5 后 `rg` 无 handler、command 属性或注册残留；`local_project_plan` 与 `apply_local_plan` 继续服务当前启动 | 无必须保留的数据/迁移职责 | `source_snapshots::tests`；本机 `local_` 过滤测试 | `delete` | 已删除旧独立预览准备 IPC；保留当前 `start_local_preview` 的共享 plan/apply 内核。 |
| `prepare_managed_server_deployment`      | `apps/desktop/src/features/deployment-editor/deployment-editor-services.ts`                                                                                                                                                                                                       | `prepareManagedServerDeployment` in `apps/desktop/src/api.ts`               | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | Deployment aggregate、线路、current pointer 或恢复事实 | server-deployment/DeploymentEditor Feature tests; `src-tauri/src/tests.rs`; Workspace tests                                                       | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `prepare_managed_server_environment`     | `apps/desktop/src/features/deployment-editor/deployment-editor-services.ts`                                                                                                                                                                                                       | `prepareManagedServerEnvironment` in `apps/desktop/src/api.ts`              | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | 连接/秘密引用；秘密正文保持 Keychain 边界              | server-deployment/DeploymentEditor Feature tests; `src-tauri/src/tests.rs`; Workspace tests                                                       | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `prepare_pipeline_identity`              | `apps/desktop/src/features/deployment-editor/server-deployment-authorization.ts`                                                                                                                                                                                                  | `preparePipelineIdentity` in `apps/desktop/src/api.ts`                      | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | —                                                      | `apps/desktop/src/api.test.ts`; adjacent Feature tests; `src-tauri/src/tests.rs`                                                                  | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `preview_manifest` | 无 | — | no | Task 4 后 `rg` 无 handler、command 属性或注册残留 | 无必须保留的数据/迁移职责；共享校验/规划内核仍由当前入口使用 | `cargo test -p abcdeploy-desktop manifest -- --nocapture`（4 PASS）；Manifest 注入防护测试 | `delete` | 已删除旧预览 IPC；保留 `open_project`、`save_manifest_draft` 与共享 Manifest 校验/规划内核。 |
| `project_managed_deployment_evidence`    | `apps/desktop/src/features/deployment-editor/deployment-editor-services.ts`                                                                                                                                                                                                       | `projectManagedDeploymentEvidence` in `apps/desktop/src/api.ts`             | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | Deployment aggregate、线路、current pointer 或恢复事实 | server-deployment/DeploymentEditor Feature tests; `src-tauri/src/tests.rs`; Workspace tests                                                       | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `promote_production_deployment`          | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | Task 6 后 `rg` 无 endpoint、审批或 promote 专属实现残留                                                                                                                                           | 无必须保留的数据/迁移职责                              | route/server_deploy 共享行为测试                                                                                          | `delete`      | 已删除旧 production promote 控制面；当前不可变版本与部署恢复链不变。                |
| `reapply_deployment_routes`              | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | Task 6 后 `rg` 无 endpoint 或整站 route 重应用实现残留                                                                                                                                           | 无必须保留的数据/迁移职责                              | route/server_deploy 共享行为测试                                                                                          | `delete`      | 已删除旧重应用入口；当前 `check_deployment_routes` 和 deployment-path 精确修复链保持。                |
| `recommend_runtime_config` | 无 | — | no | Task 5 后 `rg` 无 handler、command 属性、注册或专属 profile→runtime 推荐链残留 | 无必须保留的数据/迁移职责；当前 `load_runtime_config` / `store_runtime_config` 保持不变 | Rust workspace/Clippy 门禁；90/45/45 命令面审计 | `delete` | 已删除旧配置推荐 IPC 与唯一保活的 profile 转换、本机数据库创建辅助链。 |
| `redeploy_deployment_path_version`       | `apps/desktop/src/features/deployment-detail/deployment-detail-services.ts`                                                                                                                                                                                                       | `redeployDeploymentPathVersion` in `apps/desktop/src/api.ts`                | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | Deployment aggregate、线路、current pointer 或恢复事实 | `deployment-detail/model.test.ts`; `ProjectGallery.test.tsx`; Workspace/Tauri tests                                                               | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `refresh_deployment`                     | `apps/desktop/src/features/deployment-editor/deployment-editor-services.ts`<br>`apps/desktop/src/features/deployment-list/use-deployment-dashboard.ts`                                                                                                                            | `refreshDeployment` in `apps/desktop/src/api.ts`                            | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | Deployment aggregate、线路、current pointer 或恢复事实 | `use-deployment-dashboard.test.ts`; `ProjectGallery.test.tsx`; Workspace tests                                                                    | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `relink_project`                         | `apps/desktop/src/App.tsx`                                                                                                                                                                                                                                                        | `relinkProject` in `apps/desktop/src/api.ts`                                | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | Deployment aggregate、线路、current pointer 或恢复事实 | `apps/desktop/src/api.test.ts`; adjacent Feature tests; `src-tauri/src/tests.rs`                                                                  | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `replace_registry_credentials`           | `apps/desktop/src/features/deployment-editor/server-deployment-authorization.ts`                                                                                                                                                                                                  | `replaceRegistryCredentials` in `apps/desktop/src/api/secrets.ts`           | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | 连接/秘密引用；秘密正文保持 Keychain 边界              | server-deployment/DeploymentEditor Feature tests; `src-tauri/src/tests.rs`; Workspace tests                                                       | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `reset_project_deployment`               | `apps/desktop/src/features/deployment-editor/deployment-editor-services.ts`<br>`apps/desktop/src/features/deployment-editor/use-deployment-adoption.ts`                                                                                                                           | `resetProjectDeployment` in `apps/desktop/src/api.ts`                       | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | Deployment aggregate、线路、current pointer 或恢复事实 | server-deployment/DeploymentEditor Feature tests; `src-tauri/src/tests.rs`; Workspace tests                                                       | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `resolve_local_folder_source`            | `apps/desktop/src/features/deployment-editor/deployment-editor-services.ts`                                                                                                                                                                                                       | `resolveLocalFolderSource` in `apps/desktop/src/api.ts`                     | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | 不可变源码/本机运行身份与证据                          | `DeploymentEditorController.test.tsx`; `source_snapshots.rs` tests; `deployment-evidence/model.test.ts`                                           | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `resolve_managed_server_environment`     | `apps/desktop/src/features/deployment-editor/deployment-editor-services.ts`                                                                                                                                                                                                       | `resolveManagedServerEnvironment` in `apps/desktop/src/api.ts`              | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | 连接/秘密引用；秘密正文保持 Keychain 边界              | server-deployment/DeploymentEditor Feature tests; `src-tauri/src/tests.rs`; Workspace tests                                                       | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `resume_staging_deployment`              | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | Task 6 后 `rg` 无 endpoint 或旧 staging 续跑实现残留                                                                                                                                           | 无必须保留的数据/迁移职责                              | route/server_deploy 共享行为测试                                                                                          | `delete`      | 已删除旧 staging resume 入口；当前 refresh/redeploy 恢复链保持。                |
| `retry_deployment_certificates`          | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | Task 6 后 `rg` 无 endpoint、证书重试脚本或专属 helper 残留                                                                                                                                           | 无必须保留的数据/迁移职责                              | route/server_deploy 共享行为测试                                                                                          | `delete`      | 已删除旧证书重试控制面；当前公开路由失败仍按可恢复状态暂停。                |
| `rollback_environment`                   | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | Task 6 后 `runtime_config.rs` 中无 endpoint、rollback 脚本或专属实现残留                                                                                                                                           | 无必须保留的数据/迁移职责                              | route/server_deploy 与 Workspace 当前指针测试                                                                                          | `delete`      | 已删除旧 environment rollback 控制面；未删除迁移/current pointer 事实。                |
| `runtime_config_sync_status`             | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | Workspace/runtime_config 保护测试；无独立 endpoint 存在性要求                                                                                     | `delete`      | 旧数据/配置/秘密/Provider 管理 endpoint 无生产消费者；未列入 internalize 的实现无当前主线或迁移职责。                           |
| `runtime_secret_status`                  | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | Workspace/runtime_config 保护测试；无独立 endpoint 存在性要求                                                                                     | `delete`      | 旧数据/配置/秘密/Provider 管理 endpoint 无生产消费者；未列入 internalize 的实现无当前主线或迁移职责。                           |
| `save_config_profile`                    | 无；Task 3 已删除无生产消费者的 API 包装与 `config-profiles.ts`                                                                                                                                                                                          | —                                                                           | no                | `WorkspaceState::save_config_profile` 仅由 `workspace/tests.rs` 的兼容场景调用，并以精确 `#[cfg(test)]` 保留                                                                                  | 旧 Profile/绑定迁移夹具接缝；无生产 Profile CRUD 职责 | `stores_reusable_profiles_and_project_bindings_without_secret_values`; `upgrades_profile_bindings_and_supports_multiple_profiles_per_environment`; 项目移动/重置兼容测试 | `internalize` | 已删除 handler 与本机基础设施生产 caller 说法；只保留 `WorkspaceState::save_config_profile` test-only 兼容接缝。                                            |
| `save_deployment_path`                   | `apps/desktop/src/features/deployment-editor/server-deployment-setup-service.ts`                                                                                                                                                                                                  | `saveDeploymentPath` in `apps/desktop/src/api.ts`                           | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | Deployment aggregate、线路、current pointer 或恢复事实 | server-deployment/DeploymentEditor Feature tests; `src-tauri/src/tests.rs`; Workspace tests                                                       | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `save_manifest_draft`                    | `apps/desktop/src/features/deployment-editor/server-deployment-authorization.ts`                                                                                                                                                                                                  | `saveManifestDraft` in `apps/desktop/src/api.ts`                            | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | —                                                      | server-deployment/DeploymentEditor Feature tests; `src-tauri/src/tests.rs`; Workspace tests                                                       | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `save_project_step` | 无 | — | no | Task 4 已删除 handler；`pilot_validation::run_pilot_validation` 与部署恢复路径继续调用 `WorkspaceState::set_project_step` | 旧项目 adoption/恢复步骤兼容 | `persists_pending_managed_and_fresh_draft_adoption_states_across_restarts`; `reset_clears_only_project_deployment_state_and_blocks_stale_resurrection` | `internalize` | 已删除 IPC 边界；保留 `WorkspaceState::set_project_step` 供 pilot 与当前部署恢复路径写入步骤。 |
| `secret_status`                          | 无；仅 API 再导出/包装                                                                                                                                                                                                                                                            | `getSecretStatus` in `apps/desktop/src/api/secrets.ts`                      | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | `apps/desktop/src/api.test.ts` 的旧包装/浏览器契约；无 Tauri Feature 行为保护                                                                     | `delete`      | 包装无 Feature caller并被生产 tree-shaking 移除；无独立 Rust caller 或必须保留的数据职责。                                      |
| `set_app_setting`                        | `apps/desktop/src/features/deployment-editor/server-deployment-authorization.ts`                                                                                                                                                                                                  | `setAppSetting` in `apps/desktop/src/api/settings.ts`                       | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | 应用非秘密设置                                         | `apps/desktop/src/api.test.ts`; adjacent Feature tests; `src-tauri/src/tests.rs`                                                                  | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `set_environment_config_bindings`        | 无；仅 API 再导出/包装                                                                                                                                                                                                                                                            | `setEnvironmentConfigBindings` in `apps/desktop/src/api/config-profiles.ts` | no                | `WorkspaceState::set_environment_config_bindings`（兼容 schema 测试直接调用）                                                                                                      | 旧 profile binding schema 幂等迁移                     | `upgrades_profile_bindings_and_supports_multiple_profiles_per_environment`                                                                        | `internalize` | 删除 handler；保留 `WorkspaceState::set_environment_config_bindings` 的旧 binding schema 幂等迁移职责。                         |
| `set_local_infrastructure_service` | 无 | — | no | Task 5 后 `rg` 无 handler、command 属性、注册或单服务 Docker 控制实现残留 | 无必须保留的数据/迁移职责；Keychain secret 内核仍由当前 runtime config 链使用 | Rust workspace/Clippy 门禁；90/45/45 命令面审计 | `delete` | 已删除旧本机基础服务启停 IPC；未删除当前运行配置所需秘密辅助能力。 |
| `set_version_validation`                 | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | `WorkspaceState::set_version_validation`（版本验证事务测试直接调用）                                                                                                               | 版本验证原子写入并关联 immutable version               | `persists_version_validation_across_workspace_restart`                                                                                            | `internalize` | 删除 handler；保留 `WorkspaceState::set_version_validation` 的验证结果与 immutable version 原子关联职责。                       |
| `start_deployment_path`                  | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | Task 6 后仅保留 `start_deployment_path_inner`；由 `pilot_validation::run_pilot_validation` 调用                                                                                                        | 当前 pilot 的 build/registry/deploy 恢复链             | server deploy tests; `run_pilot_validation` path                                                                                                  | `internalize` | 已删除 Tauri adapter 与注册；保留 `start_deployment_path_inner` 的 build/registry/deploy 恢复链职责。                                          |
| `start_local_preview`                    | `apps/desktop/src/features/deployment-editor/deployment-editor-services.ts`                                                                                                                                                                                                       | `startLocalPreview` in `apps/desktop/src/api.ts`                            | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | 不可变源码/本机运行身份与证据                          | `DeploymentEditorController.test.tsx`; `source_snapshots.rs` tests; `deployment-evidence/model.test.ts`                                           | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `start_local_preview_service` | 无 | — | no | Task 5 后 `rg` 无 handler、command 属性或注册残留；构建恢复、端口探测、Compose 启动与进程跟踪继续由 `start_local_preview` 调用 | 无必须保留的数据/迁移职责 | `bulk_local_start_skips_services_without_a_reliable_build`；本机 `local_` 过滤测试 | `delete` | 已删除旧单服务启动 IPC；保留当前批量启动入口及共享构建/进程内核。 |
| `start_staging_deployment`               | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | Task 6 后 `rg` 无 endpoint 或旧 staging 启动实现残留                                                                                                                                           | 无必须保留的数据/迁移职责                              | route/server_deploy 共享行为测试                                                                                          | `delete`      | 已删除旧 staging start 入口；当前 managed-server/deployment-path 入口不变。                |
| `stop_local_preview` | 无 | — | no | Task 5 后 `rg` 无 handler、command 属性、注册或旧 Compose-down 实现残留；应用退出仍调用 `stop_local_start_processes` | 无必须保留的数据/迁移职责 | `retained_local_run_cancellation_stops_a_running_child_process`；应用退出清理调用点审计 | `delete` | 已删除旧预览停止 IPC；保留受管启动子进程的取消、强制终止与退出清理。 |
| `stop_local_preview_service` | 无 | — | no | Task 5 后 `rg` 无 handler、command 属性、注册或旧单服务 Compose-stop 实现残留 | 无必须保留的数据/迁移职责；共享进程退出清理保持 | 取消/超时具名测试；Rust workspace/Clippy 门禁 | `delete` | 已删除旧单服务停止 IPC；当前受管本机运行与退出清理链不变。 |
| `stop_managed_local_port_owner` | 无 | — | no | Task 5 后 `rg` 无 handler、command 属性、注册或 Docker-stop adapter 残留；`managed_local_port_owner` 仍由当前启动端口检查调用 | 无必须保留的数据/迁移职责 | `recognizes_only_abcdeploy_managed_development_port_owners`；本机 `local_` 过滤测试 | `delete` | 已删除旧主动停止端口 owner IPC；保留当前启动所需只读 owner 识别与冲突保护。 |
| `store_runtime_config`                   | `apps/desktop/src/features/deployment-editor/deployment-editor-services.ts`<br>`apps/desktop/src/features/deployment-editor/use-runtime-configuration-recovery.ts`                                                                                                                | `storeRuntimeConfig` in `apps/desktop/src/api.ts`                           | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | Deployment aggregate、线路、current pointer 或恢复事实 | server-deployment/DeploymentEditor Feature tests; `src-tauri/src/tests.rs`; Workspace tests                                                       | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `store_runtime_secret`                   | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | Workspace/runtime_config 保护测试；无独立 endpoint 存在性要求                                                                                     | `delete`      | 旧数据/配置/秘密/Provider 管理 endpoint 无生产消费者；未列入 internalize 的实现无当前主线或迁移职责。                           |
| `store_secret`                           | 无；仅 API 再导出/包装                                                                                                                                                                                                                                                            | `storeSecret` in `apps/desktop/src/api/secrets.ts`                          | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | `apps/desktop/src/api.test.ts` 的旧包装/浏览器契约；无 Tauri Feature 行为保护                                                                     | `delete`      | 包装无 Feature caller并被生产 tree-shaking 移除；无独立 Rust caller 或必须保留的数据职责。                                      |
| `sync_external_deployments`              | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | Task 6 后 `rg` 无 endpoint、旧 CNB 历史导入或专属同步 helper 残留                                                                                                                                           | 无必须保留的数据/迁移职责                              | 当前 Workspace/refresh 测试                                                                                          | `delete`      | 已删除旧外部历史导入控制面；当前部署刷新与持久化链不变。                |
| `sync_project_to_cnb`                    | `apps/desktop/src/features/deployment-editor/deployment-editor-services.ts`                                                                                                                                                                                                       | `syncProjectToCnb` in `apps/desktop/src/api.ts`                             | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | Deployment aggregate、线路、current pointer 或恢复事实 | server-deployment/DeploymentEditor Feature tests; `src-tauri/src/tests.rs`; Workspace tests                                                       | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `sync_runtime_config_to_server`          | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | 无；`rg` 仅命令定义、导入或 handler 注册                                                                                                                                           | 无必须保留的数据/迁移职责                              | Workspace/runtime_config 保护测试；无独立 endpoint 存在性要求                                                                                     | `delete`      | 旧数据/配置/秘密/Provider 管理 endpoint 无生产消费者；未列入 internalize 的实现无当前主线或迁移职责。                           |
| `take_over_deployment_path_routes`       | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | Task 6 后仅保留 `take_over_deployment_path_routes_inner`；由 `pilot_validation::run_pilot_validation` 调用                                                                                             | 路由接管后只续验证并保留既有制品                       | `persists_per_address_route_checks_across_workspace_restart`; `repaired_deployment_path_routes_keep_existing_artifacts_on_the_server`             | `internalize` | 已删除 Tauri adapter 与注册；保留 `take_over_deployment_path_routes_inner` 作为 pilot 只续路由验证并复用既有制品的内部路径。                 |
| `take_over_server_routes`                | 无                                                                                                                                                                                                                                                                                | —                                                                           | no                | Task 6 后仅保留 `take_over_caddy_routes`；由 `take_over_deployment_path_routes_inner` 复用                                                                                                             | 共享 Caddy 原子接管、校验与恢复实现                    | route takeover/rollback tests; `repaired_deployment_path_routes_keep_existing_artifacts_on_the_server`                                            | `internalize` | 已删除 Tauri adapter 与注册；保留 `take_over_caddy_routes` 作为共享 Caddy 原子接管、校验与恢复实现。                   |
| `verify_managed_local_run`               | `apps/desktop/src/features/deployment-editor/deployment-editor-services.ts`                                                                                                                                                                                                       | `verifyManagedLocalRun` in `apps/desktop/src/api.ts`                        | yes               | 仅 Tauri handler 边界；内部下调 Workspace/Provider                                                                                                                                 | Deployment aggregate、线路、current pointer 或恢复事实 | `DeploymentEditorController.test.tsx`; `source_snapshots.rs` tests; `deployment-evidence/model.test.ts`                                           | `keep-ipc`    | 当前 Feature → TypeScript wrapper → production bundle → Rust handler 链完整，保留 IPC。                                         |
| `write_local_env` | 无 | — | no | Task 5 后 `rg` 无 handler、command 属性、注册、`LocalEnvWriteResult` 或 `write_project_local_env` 残留 | 无必须保留的数据/迁移职责；`write_container_runtime_env` 仍由当前启动使用 | `container_runtime_uses_the_host_gateway_without_changing_other_env_values`；本机 `local_` 过滤测试 | `delete` | 已删除会写用户项目 `.env` 的旧 IPC 及专属备份/ignore 辅助链；保留受管工作区运行时 env 转换。 |
